# Travel Playbook — Fix Plan for the 37 audited issues

Base dir: `/Users/avinashvankadaru/conductor/workspaces/agents-auto-research/tokyo-v1/travel/`
Rule for every implementer: **surgical anchor-replace edits only. Preserve surrounding text, headers, and style. Apply ONLY the fixes in your unit. No scope creep, no "while I was here" edits. Delete-before-add: most fixes REMOVE a restated literal/enum and replace it with a name/slug cross-ref.** Read the file first, then Edit.

## CANON (shared exact wordings — use verbatim so coupled docs stay consistent)

**CANON-CHANNEL13** — the channel-sub-type invalidation case (used in 3 docs):
- REGISTRY-PROTOCOL.md, add a bullet right after the `New lens/archetype promoted` bullet (the axis/lens bullets list in INVALIDATION):
  `- New **channel** sub-type promoted → every already-swept theme is \`dirty\` on the CHANNEL axis restricted to the new sub-type id. Re-sweep **only that channel sub-type** for each theme (targeted, exactly like the axis case).`
- 04-discovery-loop.md:31 — generalize the existing sentence so it covers channel too. Change `mark every already-swept theme \`dirty\` on the new axis and re-sweep that axis for them` → `mark every already-swept theme \`dirty\` on the new axis (or, for a promoted **channel** sub-type, on the CHANNEL axis restricted to that sub-type) and re-sweep only the new entry for them`.
- 09-agent-orchestration.md:20 — add a third dispatch case in the `Dirty-unit re-sweep` pattern, after `a new lens/archetype → re-run the seed-completeness diff`: `; a new **channel** sub-type → one agent per already-swept theme sweeping just that channel sub-type id on the CHANNEL axis`.

**CANON-WITHDRAWN** — the new row.status value (tags-registry adds it; 08 references it by name):
- tags-registry.md row.status table: add row `| \`withdrawn\` | tour/departure confirmed pulled by the operator on a VERIFY re-fetch (was live, now removed) |`.
- tags-registry.md PROMOTION LOG: add `- (L8 / Italy freshness) \`withdrawn\` added to row.status — a VERIFY re-fetch can confirm a departure was pulled.`

---

## U-channel13 — files: REGISTRY-PROTOCOL.md, 04-discovery-loop.md, 09-agent-orchestration.md
1. **[13] REGISTRY-PROTOCOL.md:63-67** — apply CANON-CHANNEL13 (add the channel bullet). The header at line 63 already names "axis/lens/archetype/channel"; this makes the channel half operative.
2. **[3] REGISTRY-PROTOCOL.md:48-60** — the "Lead types → destination (routing table)" is a growable enumeration with no evolve-wire. (a) Tag the table heading: `- **Lead types → destination (routing table)** (open — append on discovery; REGISTRY-PROTOCOL.md):`. (b) Add provenance to the table: append a trailing note line under the table `Provenance: base rows L22; \`composition-pattern\` row L26. Append a row + its Lnn/country when a new lead type surfaces.` (c) At line 28 OPEN ENUMERATIONS catalogue, add `lead types` to the list of growable-list kinds.
3. **[8] 09-agent-orchestration.md:29** — drop the inline re-listing of the three de-dup guards; keep the cross-ref. Change `close its file with a **de-dup guards** note (\`corpus\`): aggregators excluded, sub-brands collapsed, prior-captured excluded (alias/exclusion authority = \`operator-aliases\`).` → `close its file with the **de-dup guards** note defined in \`corpus\` (alias/exclusion authority = \`operator-aliases\`).`
4. **[13] 09-agent-orchestration.md:20** — apply CANON-CHANNEL13 (add the channel dispatch case).
5. **[13] 04-discovery-loop.md:31** — apply CANON-CHANNEL13 (generalize the sentence).

## U-corpusFINAL16 — files: 05-convergence-and-admission-bar.md, 06-corpus-and-persistence.md, 08-freshness-and-updates.md, doc-manifest.md
Coherent target lifecycle (single-source it): **corpus_FINAL.md is produced by freshness consolidation (post-convergence)**; the **convergence per-axis dry tracker + CONVERGED statement live in `<country>_theme_map_FINAL.md`** (produced by admission-bar); **admission-bar reads the per-round corpus files**, not corpus_FINAL. Keep doc-manifest.md:52 producer = freshness (authoritative).
1. **[16] 05:6** — INPUT wrongly attributes corpus_FINAL to discovery-loop and reads a not-yet-existing file. Change the INPUT line `\`<country>/corpus_FINAL.md\` — per-theme operator/product rows with axis dry-status (from the discovery-loop step).` → `the per-round corpus files \`<country>/corpus/round<N>_<cluster>.md\` — per-theme operator/product rows (from the discovery-loop step). (corpus_FINAL.md is the later consolidated/locked corpus, produced by freshness — not read here.)`. Then in 05:20 and 05:25 replace reads/writes of `corpus_FINAL.md` with the round corpus files for reading, and write the per-axis dry tracker into `<country>_theme_map_FINAL.md` (the convergence artifact 05 already owns). Verify the change is internally consistent with 05's OUTPUT (theme_map_FINAL).
2. **[8] 05:24 and 05:39** — both restate the promotion-test conditions that 04:54 declares single-sourced. Reduce each to a bare cross-ref. 05:24 `apply the promotion test owned by the discovery-loop step (cross-ref via \`doc-manifest.md\`): a **standalone multi-day spine** AND a **distinct buyer + supplier base** (bare non-overlap is insufficient).` → `apply the sub-tag→theme promotion test owned by \`discovery-loop\` (resolve via \`doc-manifest.md\`); bare non-overlap is insufficient.` 05:39 `PROMOTE sub-tag → theme IFF non-overlapping AND has a standalone multi-day spine AND has a distinct buyer + supplier base (test owned by the discovery-loop step; resolve via \`doc-manifest.md\`).` → `PROMOTE sub-tag → theme IFF it passes the promotion test owned by \`discovery-loop\` (resolve via \`doc-manifest.md\`).`
3. **[9] 06:34** — drop the inline format-class value list; reference by name. `Tag each row's \`format-class\` — values + rankability in \`tags-registry.md\` (\`fixed-departure-group\` / \`private-bespoke\` / \`day-format\` / \`hybrid-course\`). Only \`fixed-departure-group\` is admissible on the "dated departure" basis directly (ranking).` → `Tag each row's \`format-class\` per \`tags-registry.md\` format-class (values + rankability owned there); only the rankable-on-dated-departure class is admissible on that basis directly (ranking).`
4. **[9] 06:36** — drop inline status value list. `Set each row's \`status\` — values \`tags-registry.md\` row.status (\`verified\` / \`UNVERIFIED\` / \`stale\`); rules below.` → `Set each row's \`status\` per \`tags-registry.md\` row.status (rules below).`
5. **[8] 06:82** — drop restated grammar literal. `Format: 2-letter country code + sequential number (e.g. \`IT-01\`).` → `Format: per \`THEME_ID_GRAMMAR\` (\`travel-config.md\`), e.g. \`IT-01\`.`
6. **[8] 06:84** — drop restated split-suffix literal. `On SPLIT → keep the parent number, append lowercase letters: \`IT-05a\` (Umbria art) / \`IT-05b\` (St Francis).` → `On SPLIT → per \`THEME_ID_GRAMMAR\` (\`travel-config.md\`): keep the parent number + lowercase suffix (e.g. Umbria art / St Francis).`
7. **[8] 06:86** — drop the overflow paraphrase. `Per THEME_ID_OVERFLOW, a country exceeding the two-digit range widens digits rather than recycling.` → `Per \`THEME_ID_OVERFLOW\` (\`travel-config.md\`).` (keep the preceding "next free number (don't reuse)" sentence.)
8. **[9] 08:21** — fix status set narration + drift. `Update \`last_checked\` to today (\`YYYY-MM-DD\`). Set \`status\` ∈ {verified, stale, withdrawn}.` → `Update \`last_checked\` to today (\`YYYY-MM-DD\`). Set \`status\` per \`tags-registry.md\` row.status.` (relies on CANON-WITHDRAWN being added to the registry by U-tags.)
9. **[9] 08:70** — example uses off-registry status; now `withdrawn` is a registry value (via U-tags), so the example is valid; leave the value but ensure surrounding text doesn't re-list the set. No change needed beyond confirming consistency; if it implies the set, reference the registry.
10. **[8] 08:44** — stop re-encoding the admission weight mapping. `an out-of-season-only or now-UNVERIFIED-date product counts at \`PARTIAL_PRODUCT_WEIGHT\` (travel-config.md) until a current-season dated departure is re-confirmed.` → `an out-of-season-only product becomes UNVERIFIED-date until a current-season dated departure is re-confirmed; its credited weight then follows the rule owned by \`admission-bar\` (\`ranking\`).`
11. **[10] 08:71** — stale retired path. `write a changelog entry to \`italy/italy_changelog.md\`.` → `write a changelog entry to \`italy/ledger.md\` (the changelog section of the single per-country ledger).`
12. **doc-manifest.md** — only edit if needed to keep the corpus_FINAL producer/lifecycle coherent; manifest:52 producer=freshness is correct, so likely NO edit. Do not introduce changes that contradict it.

## U-tags — files: tags-registry.md
1. **[9 coupled] CANON-WITHDRAWN** — add the `withdrawn` row.status value + PROMOTION LOG line.
2. **[12] tags-registry.md:37** — add inline provenance to the non-qualifying-operator disqualifier. `- **non-qualifying operator** = aggregator/marketplace reselling others (count the underlying operator; see \`operator-aliases.md\`).` → append ` (provenance: Italy R4)` to match its two siblings.

## U-02-03 — files: 02-theme-seeding.md, 03-coverage-matrix.md
1. **[11] 02:15** — give the region set a committed home. `2. List every first-level admin region (the \`region\` axis as a checklist).` → `2. READ/confirm the first-level admin regions for this country from \`<country>/axes.md\` (the \`region\`-axis values; append any new region on discovery) — do not enumerate from memory.`
2. **[8] 02:35** — drop restated split-suffix literal. `on later SPLIT, keep parent number + append lowercase suffix (\`IT-05a\`/\`IT-05b\`); never renumber (\`corpus\`).` → `on later SPLIT follow \`THEME_ID_GRAMMAR\` (\`travel-config.md\`); never renumber (\`corpus\`).`
3. **[9] 02:22** — drop inline strength value list. `Assign each theme a **strength guess** — \`tags-registry.md\` theme.strength (\`Strong\` | \`Medium\` | \`Thin\`; a guess, discovery confirms).` → `Assign each theme a **strength guess** per \`tags-registry.md\` theme.strength (a guess; discovery confirms).`
4. **[11] 03:26** — region from file. `   - **REGION** (\`region\`): every first-level admin region as a checklist.` → `   - **REGION** (\`region\`): the country's first-level admin regions, READ from \`<country>/axes.md\` (per-country \`region\`-axis values), not recalled.`
5. **[11] 03:47** — give region a committed home in AXIS VALUES. `- **REGION** (\`region\`) → every first-level admin region of \`<country>\` as a checklist. A region with 0 themes must be explicitly tested-and-justified.` → `- **REGION** (\`region\`) → the country's first-level admin regions are per-country *data* in \`<country>/axes.md\` (read at session start, appended on discovery), parallel to the LANGUAGE axis. A region with 0 themes must be explicitly tested-and-justified.`

## U-registries — files: axes-registry.md, sources-registry.md, theme-archetypes.md, operator-aliases.md, lens-registry.md
1. **[10] axes-registry.md:11** — `seed\` (used at theme seeding \`02\`) · \`discovery\` (run in discovery sweeps \`04\`)` → replace `02`→`theme-seeding`, `04`→`discovery-loop`.
2. **[10] axes-registry.md:12** — `weighted at ranking saturation \`07\`` → `07`→`ranking`.
3. **[10] axes-registry.md:24** — `advisory sanity-check (\`02\`)` → `02`→`theme-seeding`.
4. **[12] axes-registry.md PROMOTION LOG (line ~37-38)** — add a seed line before the Italy R5 line: `- (seed) Baseline axes (channel, lens, region) established from the initial cross-country method.`
5. **[10] sources-registry.md:3** — `country ranking (\`01\`)` → `01`→`country-ranking`.
6. **[10] theme-archetypes.md:3** — `Seeding (\`02\`) checks each archetype` → `02`→`theme-seeding`.
7. **[10] operator-aliases.md:3** — `the cumulative known-operator list (\`09\`)` → `09`→`orchestration`.
8. **[12] lens-registry.md PROMOTION LOG (line 24)** — add a seed line before the Italy R2 line: `- (seed) Baseline lenses established from the initial cross-country pattern scan.`

## U-readme — files: README.md
1. **[6] README.md:16** — add the no-dirty leg + stop restating the definition. `Stop only when a fresh adversarial critic adds nothing clearing the bar — and only when every axis tagged \`role:convergence-gate\` (\`axes-registry.md\`) returns dry. Convergence is "all convergence-gate axes dry," never a frozen axis count.` → `Stop only when a fresh adversarial critic adds nothing clearing \`ADMISSION_BAR\`, AND every axis tagged \`role:convergence-gate\` (\`axes-registry.md\`) returns dry, AND zero units carry a \`dirty\` flag (\`DONE\`, \`travel-config.md\`; \`REGISTRY-PROTOCOL.md\` INVALIDATION). Never a frozen axis count.`
2. **[6] README.md:67** — make the run-order operator-converged gloss match the dial + add a DONE gate. `Operator-converged = \`OPERATOR_CONVERGED\`: every axis tagged \`role:convergence-gate\` returns dry.` → `Operator-converged = \`OPERATOR_CONVERGED\` (\`travel-config.md\`): every baseline axis returns dry AND the theme carries no \`dirty\` flag.` And in step 5 (line 68) prepend a DONE gate: `Country DONE only per \`DONE\` (\`travel-config.md\`): both convergences AND zero \`dirty\` units (re-sweep any dirty unit first — \`REGISTRY-PROTOCOL.md\` INVALIDATION). Then stamp corpus; register in refresh cadence (\`corpus\`, \`freshness\`).`

## U-00 — files: 00-overview-and-principles.md
1. **[16] 00** — bring into agent-spec form WITHOUT changing any content: add a `## PROCEDURE` section (ordered steps a fresh agent runs: read the named dials + registries → for each in-scope country emit the per-theme OUTPUT CONTRACT → flag any unverified leader/departure → hand off to `country-ranking` then `theme-seeding`) and a `## DECISION RULES` header grouping the existing checkable conditions (re-home/duplicate-cite the RANKING CRITERIA priority order + THEME DESIGN RULES + ADMISSION IFF + HARD RULES as `X IFF Y` / `if X → Y` checks). Do NOT delete or alter the existing DEFINITIONS / OUTPUT CONTRACT / EXAMPLE / ANTI-PATTERNS content — only add the two missing canonical section headers and minimal connective text so the form matches siblings (04/05). Keep it tight; no new rules.

## U-misc — files: 11-trip-composition.md, AUDIT-CHECKLIST.md, 01-country-ranking.md, 07-verification-and-ranking.md
1. **[10] 11-trip-composition.md:49** — `Output \`compositions/italy-sicily-history-food.md\`:` → `Output \`italy/compositions/sicily-history-food.md\`:` (canonical `<country>/compositions/<label>.md`).
2. **[10] AUDIT-CHECKLIST.md:27** — `scan EVERY document (\`00\`–\`11\`, README, all registries/config/manifest).` → `scan EVERY document (every step doc in the STEP DOCS table of \`doc-manifest.md\`, README, and all registries/config/manifest).`
3. **[4/15] 01-country-ranking.md:23** — name the committed leads file before routing. `emit it as a typed lead (authority/source) per \`REGISTRY-PROTOCOL.md\` INTELLIGENCE CAPTURE & ROUTING → \`sources-registry\` candidate. Stop.` → `APPEND a typed authority/source lead row to \`<country>/leads.md\` with provenance (source URL + run), then route it to a \`sources-registry\` candidate per \`REGISTRY-PROTOCOL.md\` INTELLIGENCE CAPTURE & ROUTING. Stop.`
4. **[14] 01-country-ranking.md:6** — versioned-contract + reconcile column header. Change OUTPUT to include `last_checked` and add a schema note. `OUTPUT: \`country_ranking.md\` — stamped table, columns: \`rank · country · arrivals · metric · data-year · source · contested? · in-scope?\`.` → `OUTPUT: \`country_ranking.md\` — stamped table, columns: \`rank · country · arrivals · metric · data-year · source · last_checked · contested? · in-scope?\`. The column set is a versioned contract: add/rename a column per \`corpus\` SCHEMA EVOLUTION (schema-version bump + backfill older additively-merged rows by re-pull on the next \`RERANK_CADENCE\` pass, else \`unknown\` sentinel).`
5. **[14] 07-verification-and-ranking.md:64-69** — declare the rankings output schema a versioned contract. After the OUTPUT SCHEMA field list, add: `This output schema is a versioned contract (same rule as the corpus row schema — \`corpus\` SCHEMA EVOLUTION): a field add/rename is lesson-tracked, the \`composition\` consumer is updated in lockstep, and older \`rankings/<theme-id>.md\` files are re-emitted (marked \`dirty\` for rebuild from the corpus) rather than left with a stale field set.`
