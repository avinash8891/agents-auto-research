# Global Operator-Aliases Registry

Append-only de-dup data: sub-brand→parent absorptions and aggregator exclusions, so the cumulative known-operator list (`09`) dedups consistently and the guard doesn't drift. Protocol: `REGISTRY-PROTOCOL.md`. Docs cite "apply the alias registry", not inline exemplars.

## PARENT / SUB-BRAND (collapse sub-brand into parent — count once)
| sub-brand | parent | note |
|-----------|--------|------|
| Dr. Tigges | Gebeco | brand absorbed |

## AGGREGATOR / RESELLER EXCLUSIONS (not operators — count the underlying operator)
| excluded | reason |
|----------|--------|
| studienreisen.de | aggregator/marketplace, resells operators |
| rundreisen.de | aggregator/marketplace |

## PROMOTION LOG
- (Italy R5) seeded from the language-axis de-dup guards.

Append a row whenever a new absorption or aggregator is discovered in any country (compounding).
