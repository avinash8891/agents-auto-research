# Implementation Plans

Generated by the improve skill on 2026-06-18. Execute in the order below unless dependencies say otherwise. Each executor should read the plan fully before starting, honor STOP conditions, and update its row when done.

## Execution Order & Status

| Plan | Title | Priority | Effort | Depends on | Status |
|---|---|---|---|---|---|
| 001 | Make packaged top-level modules match the repo | P1 | S | - | TODO |
| 002 | Make coverage measure current runtime code before ratcheting | P2 | M | 001 | TODO |
| 003 | Reconcile the stale tech debt audit | P2 | S | 001, 002 | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale).

## Dependency Notes

- 002 depends on 001 because coverage should measure the actual packaged runtime surface, not a stale hand-picked module subset.
- 003 depends on 001 and 002 because the stale audit should be reconciled after the live package and coverage state are corrected.

## Vetted Findings

| # | Finding | Category | Impact | Effort | Risk | Evidence |
|---|---|---|---|---|---|---|
| 1 | `pyproject.toml` omits 18 root modules from `tool.setuptools.py-modules`. | bug / packaging | Wheel or non-editable installs can miss modules imported by runtime code. | S | LOW | `pyproject.toml:14`; imports from `autoresearch_research.py`, `research_subagents.py`, `thesis_validator.py`; plan 001 lists the missing modules. |
| 2 | Coverage measures a legacy subset and gates at `45`. | tests | CI can pass while large current runtime areas are unmeasured. | M | MED | `pyproject.toml:118`, `pyproject.toml:143`; `TECH_DEBT_AUDIT.md` still tracks coverage debt. |
| 3 | `TECH_DEBT_AUDIT.md` contains stale contradictory TODO/FIXED claims. | docs | Future agents will chase fixed work and report false positives. | S | LOW | `TECH_DEBT_AUDIT.md` rows F001/F019 conflict with `pyproject.toml` and `backtest/runner.py`. |

## Direction Suggestions

- Pluggable agent backends are already specified in `docs/superpowers/specs/2026-05-04-pluggable-agent-backends-design.md`, while live code still centers OpenAI/OAuth proxy paths. Treat this as a product direction option, not an audit bug: the design exists, but execution should wait until current packaging and verification debt is cleaned up.
- The analyst `run_python` tool is powerful by design. If the project later runs untrusted third-party prompts, make sandboxing a dedicated security design plan; do not fold it into routine cleanup.

## Findings Considered And Rejected

- Needs-code halt validation ordering: rejected as already fixed. `autoresearch_research.py` mutates halt state before best-effort validation/compile enrichment.
- `backtest/runner.py --output-dir` defaulting to `/tmp`: rejected as stale. Live code uses `os.environ.get("AUTORESEARCH_OUTPUT_DIR", ".")`, and `tests/test_backtest_output.py` has a regression test.
- High-severity credential leak: rejected. Read-only scan found no committed `.env`; `.env.example` is present.
- Arbitrary Python execution in analyst tool: rejected as by-design for this autonomous research app, not an accidental shell injection finding.

## Audit Coverage

Audited standard-depth hotspots: root packaging/config, CI, coverage, docs/audit state, autoresearch validation/halt flow, MCP arg schemas, research conductor/subagent tool boundaries, and deployment/output defaults.

Not exhaustively audited: every strategy math path, full SQL query performance, dependency advisories from `pip-audit`, and all docs under `docs/superpowers/plans/`.
