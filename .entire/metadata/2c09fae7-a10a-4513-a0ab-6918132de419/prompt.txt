
  Goal:
  Find other bug classes in builder/implementation-verification/experiment-contract handling, especially around
  codegen, diagnostics, runtime config, and resume/retry behavior.

  Scope:
  - `compiler_builder.py`
  - `compiler_implementation_verify.py`
  - `compiler_research.py`
  - `compiler_operationalize.py`
  - `autoresearch_orchestration.py`
  - `autoresearch_research.py`
  - `autoresearch_experiment.py`
  - `research_types.py`
  - any EMA strategy/runtime files touched by builder verification paths
  - relevant tests:
    - `tests/test_compiler_pipeline_characterization.py`
    - `tests/test_autoresearch_research.py`
    - `tests/test_autoresearch_experiment.py`
    - `tests/test_autoresearch_orchestration.py`

  Review objectives:
  1. Identify bug classes, not style issues.
  2. Focus on contract drift, false positives, false negatives, and retry/resume hazards.
  3. Think like the builder is an imperfect codegen agent that:
     - may implement the mechanism but miss diagnostics
     - may emit diagnostics in the wrong layer
     - may invent config keys
     - may satisfy verifier heuristics without producing the real artifact
     - may partially succeed across attempts
  4. Prefer real edge cases over generic concerns.
  5. Ground every finding in exact code paths and explain the failure chain.
  6. Where possible, propose a minimal repro or missing test.

  Specific questions to answer:
  - Are there other places where prose is being normalized into machine contracts?
  - Does verifier still rely on source-text heuristics where artifact-level verification is needed?
  - Can comments, aliases, or dead code satisfy verifier checks incorrectly?
  - Are there diagnostics that belong to experiment-evaluation or result payloads but are still being checked
  only in source files?
  - Are there config/runtime validation gaps that can still let no-op or invalid implementations through?
  - Can builder “pass” while the mechanism is not really wired into runtime behavior?
  - Can retries hide or overwrite a partial failure in a way that leaves inconsistent artifacts?
  - Can resume/orchestration paths consume malformed or stale builder artifacts?
  - Are baseline-comparison diagnostics generally under-specified beyond the one we fixed?
  - Are there bug classes around aliases, canonical diagnostic keys, or surface ownership?
  - Are there tests missing for:
    - wrong-surface diagnostic emission
    - alias drift
    - comment-only compliance
    - partial builder success across retry
    - stale contract/thesis/result artifacts after retry
    - resumed halted thesis with updated contract schema

  Deliverable format:
  1. Findings first, ordered by severity.
  2. For each finding include:
     - severity
     - title
     - exact files/lines
     - failure scenario
     - why current tests miss it
     - suggested fix direction
  3. Then include:
     - “Missing test matrix”
     - “Edge cases I explicitly checked”
     - “Residual risks if no changes are made”

  Important:
  - Do not propose broad rewrites unless necessary.
  - Be skeptical of any logic that treats source-text presence as proof of behavior.
  - Treat builder as codegen under weak contracts and look for ways the system can be fooled.

---

where is the report?

---

• Verified the report against live code, reproed the real issues, and fixed them.

  What was real and is now fixed

  1. Diagnostic verifier still accepted the wrong shapes

  - Fixed in compiler_implementation_verify.py
  - Diagnostic verification now requires payload-key emission sites, not arbitrary string arguments
  - logger.info("key") no longer satisfies diagnostic emission
  - Registry-backed experiment-evaluation diagnostics are accepted from registered
    DiagnosticRequirementSpec(...), not random analysis code

  2. Disqualifier parsing silently accepted unparseable prose

  - Fixed in experiment_evaluator.py
  - Unparseable disqualifiers are now surfaced as unparsed_disqualifiers
  - Verdict becomes inconclusive instead of silently acting as “not triggered”
  - Added field in research_types.py

  3. Halted/resumed thesis path was dropping structured diagnostic specs

  - Fixed in autoresearch_orchestration.py
  - required_diagnostic_specs now persists through:
      - thesis.json
      - rewritten contract.json
      - ctx.current_contract

  4. Research-round thesis serialization was dropping structured diagnostic specs

  - Fixed in autoresearch_research.py
  - Both thesis-detail whitelists now include required_diagnostic_specs

  5. Baseline comparison was using results[0]

  - Fixed in autoresearch_experiment.py
  - Baseline metrics now come from baseline_tracker.latest() when available
  - Falls back safely when no tracker exists

  6. Analysis-only diagnostics were being treated as runtime obligations

  - Fixed in diagnostic_contracts.py
  - definition_check: / implementation_... style diagnostics are skipped from runtime verification

  7. VWAP dependency check was driven by thesis prose

  - Fixed in compiler_implementation_verify.py
  - VWAP dependency now keys off structured config/primitives/diagnostic tokens, not arbitrary prose like
    “anti-vwap”

  8. Missing-input enrichment silently disappeared

  - Fixed in diagnostic_contracts.py
  - Registered baseline-comparison enrichment now emits missing_inputs instead of silently skipping

  9. Legacy missing-primitives synthesis could hide bad needs-code contracts

  - Tightened in compiler_builder.py
  - _resolve_missing_primitives(...) no longer invents primitives from config_changes

  What was already stale in the report
  These were already fixed before this pass:

  - config-key consumption raw substring matching
  - per-attempt builder artifact overwrite
  - config-validation retry dead path
  - resumed contract.json staying stale
  - evaluator ignoring missing required diagnostics

  Tests added/updated

  - tests/test_compiler_pipeline_characterization.py
  - tests/test_autoresearch_orchestration.py
  - tests/test_autoresearch_research.py
  - tests/test_autoresearch_experiment.py

  Coverage now includes:

  - logger-only diagnostic rejection
  - analysis-only diagnostic skip
  - anti-VWAP prose not triggering VWAP dependency
  - resumed thesis preserving structured diagnostic specs
  - research log persistence of structured specs
  - unparsed disqualifier becomes inconclusive
  - baseline comparison using checkpoint metrics
  - missing-input enrichment surfacing explicitly

  Verification

  - ruff check ...
      - passed
  - black --check ...
      - passed
  - pytest tests/test_compiler_pipeline_characterization.py tests/test_autoresearch_orchestration.py tests/
    test_autoresearch_research.py tests/test_autoresearch_experiment.py -q
      - 161 passed can you check and confirm and other issues