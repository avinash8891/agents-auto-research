# bugs167

Current bug items tracked by the mission.

## Summary

- Total tracked items: **147**
- Open items: **141**
- Completed items: **6**
- In progress: **1**
- Pending: **140**

## 1. section1-research-loop-orchestration-bug-rlo-007

Fix audited bug rlo-007 in Research Loop Orchestration. Reproduce the exact failure first, add or update a failing targeted test or reproducer, implement the smallest fix for this one bug only, run focused validators, and confirm the original reproduction no longer fails. If live environment prerequisites for this bug are blocked (for example VPS or local OAuth proxy), reproduce the blocker explicitly and return with blocker evidence instead of claiming success. Do not treat unrelated mission-wide suite failures as blocking if this feature's assigned reproducer can run independently; report unrelated failures separately and continue with the assigned bug reproducer. If the assigned reproducer already passes on the current branch before any edits, do not manufacture a failure; instead collect proof-of-already-fixed-on-branch, run an adjacent confidence check, inspect the relevant code path, and close the feature with evidence.

- Milestone: `section1-research-loop-orchestration`
- Skill: `bug-fix-worker`
- Fulfills: VAL-RLO-007
- Status: `in_progress`

## 2. section1-research-loop-orchestration-bug-rlo-008

Fix audited bug rlo-008 in Research Loop Orchestration. Reproduce the exact failure first, add or update a failing targeted test or reproducer, implement the smallest fix for this one bug only, run focused validators, and confirm the original reproduction no longer fails. If live environment prerequisites for this bug are blocked (for example VPS or local OAuth proxy), reproduce the blocker explicitly and return with blocker evidence instead of claiming success. Do not treat unrelated mission-wide suite failures as blocking if this feature's assigned reproducer can run independently; report unrelated failures separately and continue with the assigned bug reproducer. If the assigned reproducer already passes on the current branch before any edits, do not manufacture a failure; instead collect proof-of-already-fixed-on-branch, run an adjacent confidence check, inspect the relevant code path, and close the feature with evidence.

- Milestone: `section1-research-loop-orchestration`
- Skill: `bug-fix-worker`
- Fulfills: VAL-RLO-008
- Status: `pending`

## 3. section1-research-loop-orchestration-bug-rlo-009

Fix audited bug rlo-009 in Research Loop Orchestration. Reproduce the exact failure first, add or update a failing targeted test or reproducer, implement the smallest fix for this one bug only, run focused validators, and confirm the original reproduction no longer fails. If live environment prerequisites for this bug are blocked (for example VPS or local OAuth proxy), reproduce the blocker explicitly and return with blocker evidence instead of claiming success. Do not treat unrelated mission-wide suite failures as blocking if this feature's assigned reproducer can run independently; report unrelated failures separately and continue with the assigned bug reproducer. If the assigned reproducer already passes on the current branch before any edits, do not manufacture a failure; instead collect proof-of-already-fixed-on-branch, run an adjacent confidence check, inspect the relevant code path, and close the feature with evidence.

- Milestone: `section1-research-loop-orchestration`
- Skill: `bug-fix-worker`
- Fulfills: VAL-RLO-009
- Status: `pending`

## 4. section1-research-loop-orchestration-bug-rlo-010

Fix audited bug rlo-010 in Research Loop Orchestration. Reproduce the exact failure first, add or update a failing targeted test or reproducer, implement the smallest fix for this one bug only, run focused validators, and confirm the original reproduction no longer fails. If live environment prerequisites for this bug are blocked (for example VPS or local OAuth proxy), reproduce the blocker explicitly and return with blocker evidence instead of claiming success. Do not treat unrelated mission-wide suite failures as blocking if this feature's assigned reproducer can run independently; report unrelated failures separately and continue with the assigned bug reproducer. If the assigned reproducer already passes on the current branch before any edits, do not manufacture a failure; instead collect proof-of-already-fixed-on-branch, run an adjacent confidence check, inspect the relevant code path, and close the feature with evidence.

- Milestone: `section1-research-loop-orchestration`
- Skill: `bug-fix-worker`
- Fulfills: VAL-RLO-010
- Status: `pending`

## 5. section1-research-loop-orchestration-bug-rlo-011

Fix audited bug rlo-011 in Research Loop Orchestration. Reproduce the exact failure first, add or update a failing targeted test or reproducer, implement the smallest fix for this one bug only, run focused validators, and confirm the original reproduction no longer fails. If live environment prerequisites for this bug are blocked (for example VPS or local OAuth proxy), reproduce the blocker explicitly and return with blocker evidence instead of claiming success. Do not treat unrelated mission-wide suite failures as blocking if this feature's assigned reproducer can run independently; report unrelated failures separately and continue with the assigned bug reproducer. If the assigned reproducer already passes on the current branch before any edits, do not manufacture a failure; instead collect proof-of-already-fixed-on-branch, run an adjacent confidence check, inspect the relevant code path, and close the feature with evidence.

- Milestone: `section1-research-loop-orchestration`
- Skill: `bug-fix-worker`
- Fulfills: VAL-RLO-011
- Status: `pending`

## 6. section1-research-loop-orchestration-bug-rlo-012

Fix audited bug rlo-012 in Research Loop Orchestration. Reproduce the exact failure first, add or update a failing targeted test or reproducer, implement the smallest fix for this one bug only, run focused validators, and confirm the original reproduction no longer fails. If live environment prerequisites for this bug are blocked (for example VPS or local OAuth proxy), reproduce the blocker explicitly and return with blocker evidence instead of claiming success. Do not treat unrelated mission-wide suite failures as blocking if this feature's assigned reproducer can run independently; report unrelated failures separately and continue with the assigned bug reproducer. If the assigned reproducer already passes on the current branch before any edits, do not manufacture a failure; instead collect proof-of-already-fixed-on-branch, run an adjacent confidence check, inspect the relevant code path, and close the feature with evidence.

- Milestone: `section1-research-loop-orchestration`
- Skill: `bug-fix-worker`
- Fulfills: VAL-RLO-012
- Status: `pending`

## 7. section1-research-loop-orchestration-bug-rlo-013

Fix audited bug rlo-013 in Research Loop Orchestration. Reproduce the exact failure first, add or update a failing targeted test or reproducer, implement the smallest fix for this one bug only, run focused validators, and confirm the original reproduction no longer fails. If live environment prerequisites for this bug are blocked (for example VPS or local OAuth proxy), reproduce the blocker explicitly and return with blocker evidence instead of claiming success. Do not treat unrelated mission-wide suite failures as blocking if this feature's assigned reproducer can run independently; report unrelated failures separately and continue with the assigned bug reproducer. If the assigned reproducer already passes on the current branch before any edits, do not manufacture a failure; instead collect proof-of-already-fixed-on-branch, run an adjacent confidence check, inspect the relevant code path, and close the feature with evidence.

- Milestone: `section1-research-loop-orchestration`
- Skill: `bug-fix-worker`
- Fulfills: VAL-RLO-013
- Status: `pending`

## 8. section1-research-loop-orchestration-bug-rlo-014

Fix audited bug rlo-014 in Research Loop Orchestration. Reproduce the exact failure first, add or update a failing targeted test or reproducer, implement the smallest fix for this one bug only, run focused validators, and confirm the original reproduction no longer fails. If live environment prerequisites for this bug are blocked (for example VPS or local OAuth proxy), reproduce the blocker explicitly and return with blocker evidence instead of claiming success. Do not treat unrelated mission-wide suite failures as blocking if this feature's assigned reproducer can run independently; report unrelated failures separately and continue with the assigned bug reproducer. If the assigned reproducer already passes on the current branch before any edits, do not manufacture a failure; instead collect proof-of-already-fixed-on-branch, run an adjacent confidence check, inspect the relevant code path, and close the feature with evidence.

- Milestone: `section1-research-loop-orchestration`
- Skill: `bug-fix-worker`
- Fulfills: VAL-RLO-014
- Status: `pending`

## 9. section1-research-loop-orchestration-bug-rlo-015

Fix audited bug rlo-015 in Research Loop Orchestration. Reproduce the exact failure first, add or update a failing targeted test or reproducer, implement the smallest fix for this one bug only, run focused validators, and confirm the original reproduction no longer fails. If live environment prerequisites for this bug are blocked (for example VPS or local OAuth proxy), reproduce the blocker explicitly and return with blocker evidence instead of claiming success. Do not treat unrelated mission-wide suite failures as blocking if this feature's assigned reproducer can run independently; report unrelated failures separately and continue with the assigned bug reproducer. If the assigned reproducer already passes on the current branch before any edits, do not manufacture a failure; instead collect proof-of-already-fixed-on-branch, run an adjacent confidence check, inspect the relevant code path, and close the feature with evidence.

- Milestone: `section1-research-loop-orchestration`
- Skill: `bug-fix-worker`
- Fulfills: VAL-RLO-015
- Status: `pending`

## 10. section1-research-loop-orchestration-bug-rlo-016

Fix audited bug rlo-016 in Research Loop Orchestration. Reproduce the exact failure first, add or update a failing targeted test or reproducer, implement the smallest fix for this one bug only, run focused validators, and confirm the original reproduction no longer fails. If live environment prerequisites for this bug are blocked (for example VPS or local OAuth proxy), reproduce the blocker explicitly and return with blocker evidence instead of claiming success. Do not treat unrelated mission-wide suite failures as blocking if this feature's assigned reproducer can run independently; report unrelated failures separately and continue with the assigned bug reproducer. If the assigned reproducer already passes on the current branch before any edits, do not manufacture a failure; instead collect proof-of-already-fixed-on-branch, run an adjacent confidence check, inspect the relevant code path, and close the feature with evidence.

- Milestone: `section1-research-loop-orchestration`
- Skill: `bug-fix-worker`
- Fulfills: VAL-RLO-016
- Status: `pending`

## 11. section1-research-loop-orchestration-bug-rlo-017

Fix audited bug rlo-017 in Research Loop Orchestration. Reproduce the exact failure first, add or update a failing targeted test or reproducer, implement the smallest fix for this one bug only, run focused validators, and confirm the original reproduction no longer fails. If live environment prerequisites for this bug are blocked (for example VPS or local OAuth proxy), reproduce the blocker explicitly and return with blocker evidence instead of claiming success. Do not treat unrelated mission-wide suite failures as blocking if this feature's assigned reproducer can run independently; report unrelated failures separately and continue with the assigned bug reproducer. If the assigned reproducer already passes on the current branch before any edits, do not manufacture a failure; instead collect proof-of-already-fixed-on-branch, run an adjacent confidence check, inspect the relevant code path, and close the feature with evidence.

- Milestone: `section1-research-loop-orchestration`
- Skill: `bug-fix-worker`
- Fulfills: VAL-RLO-017
- Status: `pending`

## 12. section1-research-loop-orchestration-bug-rlo-018

Fix audited bug rlo-018 in Research Loop Orchestration. Reproduce the exact failure first, add or update a failing targeted test or reproducer, implement the smallest fix for this one bug only, run focused validators, and confirm the original reproduction no longer fails. If live environment prerequisites for this bug are blocked (for example VPS or local OAuth proxy), reproduce the blocker explicitly and return with blocker evidence instead of claiming success. Do not treat unrelated mission-wide suite failures as blocking if this feature's assigned reproducer can run independently; report unrelated failures separately and continue with the assigned bug reproducer. If the assigned reproducer already passes on the current branch before any edits, do not manufacture a failure; instead collect proof-of-already-fixed-on-branch, run an adjacent confidence check, inspect the relevant code path, and close the feature with evidence.

- Milestone: `section1-research-loop-orchestration`
- Skill: `bug-fix-worker`
- Fulfills: VAL-RLO-018
- Status: `pending`

## 13. section1-research-loop-orchestration-bug-rlo-019

Fix audited bug rlo-019 in Research Loop Orchestration. Reproduce the exact failure first, add or update a failing targeted test or reproducer, implement the smallest fix for this one bug only, run focused validators, and confirm the original reproduction no longer fails. If live environment prerequisites for this bug are blocked (for example VPS or local OAuth proxy), reproduce the blocker explicitly and return with blocker evidence instead of claiming success. Do not treat unrelated mission-wide suite failures as blocking if this feature's assigned reproducer can run independently; report unrelated failures separately and continue with the assigned bug reproducer. If the assigned reproducer already passes on the current branch before any edits, do not manufacture a failure; instead collect proof-of-already-fixed-on-branch, run an adjacent confidence check, inspect the relevant code path, and close the feature with evidence.

- Milestone: `section1-research-loop-orchestration`
- Skill: `bug-fix-worker`
- Fulfills: VAL-RLO-019
- Status: `pending`

## 14. section1-research-loop-orchestration-bug-rlo-020

Fix audited bug rlo-020 in Research Loop Orchestration. Reproduce the exact failure first, add or update a failing targeted test or reproducer, implement the smallest fix for this one bug only, run focused validators, and confirm the original reproduction no longer fails. If live environment prerequisites for this bug are blocked (for example VPS or local OAuth proxy), reproduce the blocker explicitly and return with blocker evidence instead of claiming success. Do not treat unrelated mission-wide suite failures as blocking if this feature's assigned reproducer can run independently; report unrelated failures separately and continue with the assigned bug reproducer. If the assigned reproducer already passes on the current branch before any edits, do not manufacture a failure; instead collect proof-of-already-fixed-on-branch, run an adjacent confidence check, inspect the relevant code path, and close the feature with evidence.

- Milestone: `section1-research-loop-orchestration`
- Skill: `bug-fix-worker`
- Fulfills: VAL-RLO-020
- Status: `pending`

## 15. section1-research-loop-orchestration-bug-rlo-021

Fix audited bug rlo-021 in Research Loop Orchestration. Reproduce the exact failure first, add or update a failing targeted test or reproducer, implement the smallest fix for this one bug only, run focused validators, and confirm the original reproduction no longer fails. If live environment prerequisites for this bug are blocked (for example VPS or local OAuth proxy), reproduce the blocker explicitly and return with blocker evidence instead of claiming success. Do not treat unrelated mission-wide suite failures as blocking if this feature's assigned reproducer can run independently; report unrelated failures separately and continue with the assigned bug reproducer. If the assigned reproducer already passes on the current branch before any edits, do not manufacture a failure; instead collect proof-of-already-fixed-on-branch, run an adjacent confidence check, inspect the relevant code path, and close the feature with evidence.

- Milestone: `section1-research-loop-orchestration`
- Skill: `bug-fix-worker`
- Fulfills: VAL-RLO-021
- Status: `pending`

## 16. section1-research-loop-orchestration-bug-rlo-022

Fix audited bug rlo-022 in Research Loop Orchestration. Reproduce the exact failure first, add or update a failing targeted test or reproducer, implement the smallest fix for this one bug only, run focused validators, and confirm the original reproduction no longer fails. If live environment prerequisites for this bug are blocked (for example VPS or local OAuth proxy), reproduce the blocker explicitly and return with blocker evidence instead of claiming success. Do not treat unrelated mission-wide suite failures as blocking if this feature's assigned reproducer can run independently; report unrelated failures separately and continue with the assigned bug reproducer. If the assigned reproducer already passes on the current branch before any edits, do not manufacture a failure; instead collect proof-of-already-fixed-on-branch, run an adjacent confidence check, inspect the relevant code path, and close the feature with evidence.

- Milestone: `section1-research-loop-orchestration`
- Skill: `bug-fix-worker`
- Fulfills: VAL-RLO-022
- Status: `pending`

## 17. section1-research-loop-orchestration-bug-rlo-023

Fix audited bug rlo-023 in Research Loop Orchestration. Reproduce the exact failure first, add or update a failing targeted test or reproducer, implement the smallest fix for this one bug only, run focused validators, and confirm the original reproduction no longer fails. If live environment prerequisites for this bug are blocked (for example VPS or local OAuth proxy), reproduce the blocker explicitly and return with blocker evidence instead of claiming success. Do not treat unrelated mission-wide suite failures as blocking if this feature's assigned reproducer can run independently; report unrelated failures separately and continue with the assigned bug reproducer. If the assigned reproducer already passes on the current branch before any edits, do not manufacture a failure; instead collect proof-of-already-fixed-on-branch, run an adjacent confidence check, inspect the relevant code path, and close the feature with evidence.

- Milestone: `section1-research-loop-orchestration`
- Skill: `bug-fix-worker`
- Fulfills: VAL-RLO-023
- Status: `pending`

## 18. section1-research-loop-orchestration-bug-rlo-024

Fix audited bug rlo-024 in Research Loop Orchestration. Reproduce the exact failure first, add or update a failing targeted test or reproducer, implement the smallest fix for this one bug only, run focused validators, and confirm the original reproduction no longer fails. If live environment prerequisites for this bug are blocked (for example VPS or local OAuth proxy), reproduce the blocker explicitly and return with blocker evidence instead of claiming success. Do not treat unrelated mission-wide suite failures as blocking if this feature's assigned reproducer can run independently; report unrelated failures separately and continue with the assigned bug reproducer. If the assigned reproducer already passes on the current branch before any edits, do not manufacture a failure; instead collect proof-of-already-fixed-on-branch, run an adjacent confidence check, inspect the relevant code path, and close the feature with evidence.

- Milestone: `section1-research-loop-orchestration`
- Skill: `bug-fix-worker`
- Fulfills: VAL-RLO-024
- Status: `pending`

## 19. section1-research-loop-orchestration-bug-rlo-025

Fix audited bug rlo-025 in Research Loop Orchestration. Reproduce the exact failure first, add or update a failing targeted test or reproducer, implement the smallest fix for this one bug only, run focused validators, and confirm the original reproduction no longer fails. If live environment prerequisites for this bug are blocked (for example VPS or local OAuth proxy), reproduce the blocker explicitly and return with blocker evidence instead of claiming success. Do not treat unrelated mission-wide suite failures as blocking if this feature's assigned reproducer can run independently; report unrelated failures separately and continue with the assigned bug reproducer. If the assigned reproducer already passes on the current branch before any edits, do not manufacture a failure; instead collect proof-of-already-fixed-on-branch, run an adjacent confidence check, inspect the relevant code path, and close the feature with evidence.

- Milestone: `section1-research-loop-orchestration`
- Skill: `bug-fix-worker`
- Fulfills: VAL-RLO-025
- Status: `pending`

## 20. section2-agent-layer-bug-al-001

Fix audited bug al-001 in Agent Layer. Reproduce the exact failure first, add or update a failing targeted test or reproducer, implement the smallest fix for this one bug only, run focused validators, and confirm the original reproduction no longer fails. If live environment prerequisites for this bug are blocked (for example VPS or local OAuth proxy), reproduce the blocker explicitly and return with blocker evidence instead of claiming success. Do not treat unrelated mission-wide suite failures as blocking if this feature's assigned reproducer can run independently; report unrelated failures separately and continue with the assigned bug reproducer. If the assigned reproducer already passes on the current branch before any edits, do not manufacture a failure; instead collect proof-of-already-fixed-on-branch, run an adjacent confidence check, inspect the relevant code path, and close the feature with evidence.

- Milestone: `section2-agent-layer`
- Skill: `bug-fix-worker`
- Fulfills: VAL-AL-001
- Status: `pending`

## 21. section2-agent-layer-bug-al-002

Fix audited bug al-002 in Agent Layer. Reproduce the exact failure first, add or update a failing targeted test or reproducer, implement the smallest fix for this one bug only, run focused validators, and confirm the original reproduction no longer fails. If live environment prerequisites for this bug are blocked (for example VPS or local OAuth proxy), reproduce the blocker explicitly and return with blocker evidence instead of claiming success. Do not treat unrelated mission-wide suite failures as blocking if this feature's assigned reproducer can run independently; report unrelated failures separately and continue with the assigned bug reproducer. If the assigned reproducer already passes on the current branch before any edits, do not manufacture a failure; instead collect proof-of-already-fixed-on-branch, run an adjacent confidence check, inspect the relevant code path, and close the feature with evidence.

- Milestone: `section2-agent-layer`
- Skill: `bug-fix-worker`
- Fulfills: VAL-AL-002
- Status: `pending`

## 22. section2-agent-layer-bug-al-003

Fix audited bug al-003 in Agent Layer. Reproduce the exact failure first, add or update a failing targeted test or reproducer, implement the smallest fix for this one bug only, run focused validators, and confirm the original reproduction no longer fails. If live environment prerequisites for this bug are blocked (for example VPS or local OAuth proxy), reproduce the blocker explicitly and return with blocker evidence instead of claiming success. Do not treat unrelated mission-wide suite failures as blocking if this feature's assigned reproducer can run independently; report unrelated failures separately and continue with the assigned bug reproducer. If the assigned reproducer already passes on the current branch before any edits, do not manufacture a failure; instead collect proof-of-already-fixed-on-branch, run an adjacent confidence check, inspect the relevant code path, and close the feature with evidence.

- Milestone: `section2-agent-layer`
- Skill: `bug-fix-worker`
- Fulfills: VAL-AL-003
- Status: `pending`

## 23. section2-agent-layer-bug-al-004

Fix audited bug al-004 in Agent Layer. Reproduce the exact failure first, add or update a failing targeted test or reproducer, implement the smallest fix for this one bug only, run focused validators, and confirm the original reproduction no longer fails. If live environment prerequisites for this bug are blocked (for example VPS or local OAuth proxy), reproduce the blocker explicitly and return with blocker evidence instead of claiming success. Do not treat unrelated mission-wide suite failures as blocking if this feature's assigned reproducer can run independently; report unrelated failures separately and continue with the assigned bug reproducer. If the assigned reproducer already passes on the current branch before any edits, do not manufacture a failure; instead collect proof-of-already-fixed-on-branch, run an adjacent confidence check, inspect the relevant code path, and close the feature with evidence.

- Milestone: `section2-agent-layer`
- Skill: `bug-fix-worker`
- Fulfills: VAL-AL-004
- Status: `pending`

## 24. section2-agent-layer-bug-al-005

Fix audited bug al-005 in Agent Layer. Reproduce the exact failure first, add or update a failing targeted test or reproducer, implement the smallest fix for this one bug only, run focused validators, and confirm the original reproduction no longer fails. If live environment prerequisites for this bug are blocked (for example VPS or local OAuth proxy), reproduce the blocker explicitly and return with blocker evidence instead of claiming success. Do not treat unrelated mission-wide suite failures as blocking if this feature's assigned reproducer can run independently; report unrelated failures separately and continue with the assigned bug reproducer. If the assigned reproducer already passes on the current branch before any edits, do not manufacture a failure; instead collect proof-of-already-fixed-on-branch, run an adjacent confidence check, inspect the relevant code path, and close the feature with evidence.

- Milestone: `section2-agent-layer`
- Skill: `bug-fix-worker`
- Fulfills: VAL-AL-005
- Status: `pending`

## 25. section2-agent-layer-bug-al-006

Fix audited bug al-006 in Agent Layer. Reproduce the exact failure first, add or update a failing targeted test or reproducer, implement the smallest fix for this one bug only, run focused validators, and confirm the original reproduction no longer fails. If live environment prerequisites for this bug are blocked (for example VPS or local OAuth proxy), reproduce the blocker explicitly and return with blocker evidence instead of claiming success. Do not treat unrelated mission-wide suite failures as blocking if this feature's assigned reproducer can run independently; report unrelated failures separately and continue with the assigned bug reproducer. If the assigned reproducer already passes on the current branch before any edits, do not manufacture a failure; instead collect proof-of-already-fixed-on-branch, run an adjacent confidence check, inspect the relevant code path, and close the feature with evidence.

- Milestone: `section2-agent-layer`
- Skill: `bug-fix-worker`
- Fulfills: VAL-AL-006
- Status: `pending`

## 26. section2-agent-layer-bug-al-007

Fix audited bug al-007 in Agent Layer. Reproduce the exact failure first, add or update a failing targeted test or reproducer, implement the smallest fix for this one bug only, run focused validators, and confirm the original reproduction no longer fails. If live environment prerequisites for this bug are blocked (for example VPS or local OAuth proxy), reproduce the blocker explicitly and return with blocker evidence instead of claiming success. Do not treat unrelated mission-wide suite failures as blocking if this feature's assigned reproducer can run independently; report unrelated failures separately and continue with the assigned bug reproducer. If the assigned reproducer already passes on the current branch before any edits, do not manufacture a failure; instead collect proof-of-already-fixed-on-branch, run an adjacent confidence check, inspect the relevant code path, and close the feature with evidence.

- Milestone: `section2-agent-layer`
- Skill: `bug-fix-worker`
- Fulfills: VAL-AL-007
- Status: `pending`

## 27. section3-research-conductor-bug-rc-001

Fix audited bug rc-001 in Research Conductor. Reproduce the exact failure first, add or update a failing targeted test or reproducer, implement the smallest fix for this one bug only, run focused validators, and confirm the original reproduction no longer fails. If live environment prerequisites for this bug are blocked (for example VPS or local OAuth proxy), reproduce the blocker explicitly and return with blocker evidence instead of claiming success. Do not treat unrelated mission-wide suite failures as blocking if this feature's assigned reproducer can run independently; report unrelated failures separately and continue with the assigned bug reproducer. If the assigned reproducer already passes on the current branch before any edits, do not manufacture a failure; instead collect proof-of-already-fixed-on-branch, run an adjacent confidence check, inspect the relevant code path, and close the feature with evidence.

- Milestone: `section3-research-conductor`
- Skill: `bug-fix-worker`
- Fulfills: VAL-RC-001
- Status: `pending`

## 28. section3-research-conductor-bug-rc-002

Fix audited bug rc-002 in Research Conductor. Reproduce the exact failure first, add or update a failing targeted test or reproducer, implement the smallest fix for this one bug only, run focused validators, and confirm the original reproduction no longer fails. If live environment prerequisites for this bug are blocked (for example VPS or local OAuth proxy), reproduce the blocker explicitly and return with blocker evidence instead of claiming success. Do not treat unrelated mission-wide suite failures as blocking if this feature's assigned reproducer can run independently; report unrelated failures separately and continue with the assigned bug reproducer. If the assigned reproducer already passes on the current branch before any edits, do not manufacture a failure; instead collect proof-of-already-fixed-on-branch, run an adjacent confidence check, inspect the relevant code path, and close the feature with evidence.

- Milestone: `section3-research-conductor`
- Skill: `bug-fix-worker`
- Fulfills: VAL-RC-002
- Status: `pending`

## 29. section3-research-conductor-bug-rc-003

Fix audited bug rc-003 in Research Conductor. Reproduce the exact failure first, add or update a failing targeted test or reproducer, implement the smallest fix for this one bug only, run focused validators, and confirm the original reproduction no longer fails. If live environment prerequisites for this bug are blocked (for example VPS or local OAuth proxy), reproduce the blocker explicitly and return with blocker evidence instead of claiming success. Do not treat unrelated mission-wide suite failures as blocking if this feature's assigned reproducer can run independently; report unrelated failures separately and continue with the assigned bug reproducer. If the assigned reproducer already passes on the current branch before any edits, do not manufacture a failure; instead collect proof-of-already-fixed-on-branch, run an adjacent confidence check, inspect the relevant code path, and close the feature with evidence.

- Milestone: `section3-research-conductor`
- Skill: `bug-fix-worker`
- Fulfills: VAL-RC-003
- Status: `pending`

## 30. section3-research-conductor-bug-rc-004

Fix audited bug rc-004 in Research Conductor. Reproduce the exact failure first, add or update a failing targeted test or reproducer, implement the smallest fix for this one bug only, run focused validators, and confirm the original reproduction no longer fails. If live environment prerequisites for this bug are blocked (for example VPS or local OAuth proxy), reproduce the blocker explicitly and return with blocker evidence instead of claiming success. Do not treat unrelated mission-wide suite failures as blocking if this feature's assigned reproducer can run independently; report unrelated failures separately and continue with the assigned bug reproducer. If the assigned reproducer already passes on the current branch before any edits, do not manufacture a failure; instead collect proof-of-already-fixed-on-branch, run an adjacent confidence check, inspect the relevant code path, and close the feature with evidence.

- Milestone: `section3-research-conductor`
- Skill: `bug-fix-worker`
- Fulfills: VAL-RC-004
- Status: `pending`

## 31. section3-research-conductor-bug-rc-005

Fix audited bug rc-005 in Research Conductor. Reproduce the exact failure first, add or update a failing targeted test or reproducer, implement the smallest fix for this one bug only, run focused validators, and confirm the original reproduction no longer fails. If live environment prerequisites for this bug are blocked (for example VPS or local OAuth proxy), reproduce the blocker explicitly and return with blocker evidence instead of claiming success. Do not treat unrelated mission-wide suite failures as blocking if this feature's assigned reproducer can run independently; report unrelated failures separately and continue with the assigned bug reproducer. If the assigned reproducer already passes on the current branch before any edits, do not manufacture a failure; instead collect proof-of-already-fixed-on-branch, run an adjacent confidence check, inspect the relevant code path, and close the feature with evidence.

- Milestone: `section3-research-conductor`
- Skill: `bug-fix-worker`
- Fulfills: VAL-RC-005
- Status: `pending`

## 32. section3-research-conductor-bug-rc-006

Fix audited bug rc-006 in Research Conductor. Reproduce the exact failure first, add or update a failing targeted test or reproducer, implement the smallest fix for this one bug only, run focused validators, and confirm the original reproduction no longer fails. If live environment prerequisites for this bug are blocked (for example VPS or local OAuth proxy), reproduce the blocker explicitly and return with blocker evidence instead of claiming success. Do not treat unrelated mission-wide suite failures as blocking if this feature's assigned reproducer can run independently; report unrelated failures separately and continue with the assigned bug reproducer. If the assigned reproducer already passes on the current branch before any edits, do not manufacture a failure; instead collect proof-of-already-fixed-on-branch, run an adjacent confidence check, inspect the relevant code path, and close the feature with evidence.

- Milestone: `section3-research-conductor`
- Skill: `bug-fix-worker`
- Fulfills: VAL-RC-006
- Status: `pending`

## 33. section3-research-conductor-bug-rc-007

Fix audited bug rc-007 in Research Conductor. Reproduce the exact failure first, add or update a failing targeted test or reproducer, implement the smallest fix for this one bug only, run focused validators, and confirm the original reproduction no longer fails. If live environment prerequisites for this bug are blocked (for example VPS or local OAuth proxy), reproduce the blocker explicitly and return with blocker evidence instead of claiming success. Do not treat unrelated mission-wide suite failures as blocking if this feature's assigned reproducer can run independently; report unrelated failures separately and continue with the assigned bug reproducer. If the assigned reproducer already passes on the current branch before any edits, do not manufacture a failure; instead collect proof-of-already-fixed-on-branch, run an adjacent confidence check, inspect the relevant code path, and close the feature with evidence.

- Milestone: `section3-research-conductor`
- Skill: `bug-fix-worker`
- Fulfills: VAL-RC-007
- Status: `pending`

## 34. section4-compiler-pipeline-bug-cp-001

Fix audited bug cp-001 in Compiler Pipeline. Reproduce the exact failure first, add or update a failing targeted test or reproducer, implement the smallest fix for this one bug only, run focused validators, and confirm the original reproduction no longer fails. If live environment prerequisites for this bug are blocked (for example VPS or local OAuth proxy), reproduce the blocker explicitly and return with blocker evidence instead of claiming success. Do not treat unrelated mission-wide suite failures as blocking if this feature's assigned reproducer can run independently; report unrelated failures separately and continue with the assigned bug reproducer. If the assigned reproducer already passes on the current branch before any edits, do not manufacture a failure; instead collect proof-of-already-fixed-on-branch, run an adjacent confidence check, inspect the relevant code path, and close the feature with evidence.

- Milestone: `section4-compiler-pipeline`
- Skill: `bug-fix-worker`
- Fulfills: VAL-CP-001
- Status: `pending`

## 35. section4-compiler-pipeline-bug-cp-002

Fix audited bug cp-002 in Compiler Pipeline. Reproduce the exact failure first, add or update a failing targeted test or reproducer, implement the smallest fix for this one bug only, run focused validators, and confirm the original reproduction no longer fails. If live environment prerequisites for this bug are blocked (for example VPS or local OAuth proxy), reproduce the blocker explicitly and return with blocker evidence instead of claiming success. Do not treat unrelated mission-wide suite failures as blocking if this feature's assigned reproducer can run independently; report unrelated failures separately and continue with the assigned bug reproducer. If the assigned reproducer already passes on the current branch before any edits, do not manufacture a failure; instead collect proof-of-already-fixed-on-branch, run an adjacent confidence check, inspect the relevant code path, and close the feature with evidence.

- Milestone: `section4-compiler-pipeline`
- Skill: `bug-fix-worker`
- Fulfills: VAL-CP-002
- Status: `pending`

## 36. section4-compiler-pipeline-bug-cp-003

Fix audited bug cp-003 in Compiler Pipeline. Reproduce the exact failure first, add or update a failing targeted test or reproducer, implement the smallest fix for this one bug only, run focused validators, and confirm the original reproduction no longer fails. If live environment prerequisites for this bug are blocked (for example VPS or local OAuth proxy), reproduce the blocker explicitly and return with blocker evidence instead of claiming success. Do not treat unrelated mission-wide suite failures as blocking if this feature's assigned reproducer can run independently; report unrelated failures separately and continue with the assigned bug reproducer. If the assigned reproducer already passes on the current branch before any edits, do not manufacture a failure; instead collect proof-of-already-fixed-on-branch, run an adjacent confidence check, inspect the relevant code path, and close the feature with evidence.

- Milestone: `section4-compiler-pipeline`
- Skill: `bug-fix-worker`
- Fulfills: VAL-CP-003
- Status: `pending`

## 37. section4-compiler-pipeline-bug-cp-004

Fix audited bug cp-004 in Compiler Pipeline. Reproduce the exact failure first, add or update a failing targeted test or reproducer, implement the smallest fix for this one bug only, run focused validators, and confirm the original reproduction no longer fails. If live environment prerequisites for this bug are blocked (for example VPS or local OAuth proxy), reproduce the blocker explicitly and return with blocker evidence instead of claiming success. Do not treat unrelated mission-wide suite failures as blocking if this feature's assigned reproducer can run independently; report unrelated failures separately and continue with the assigned bug reproducer. If the assigned reproducer already passes on the current branch before any edits, do not manufacture a failure; instead collect proof-of-already-fixed-on-branch, run an adjacent confidence check, inspect the relevant code path, and close the feature with evidence.

- Milestone: `section4-compiler-pipeline`
- Skill: `bug-fix-worker`
- Fulfills: VAL-CP-004
- Status: `pending`

## 38. section4-compiler-pipeline-bug-cp-005

Fix audited bug cp-005 in Compiler Pipeline. Reproduce the exact failure first, add or update a failing targeted test or reproducer, implement the smallest fix for this one bug only, run focused validators, and confirm the original reproduction no longer fails. If live environment prerequisites for this bug are blocked (for example VPS or local OAuth proxy), reproduce the blocker explicitly and return with blocker evidence instead of claiming success. Do not treat unrelated mission-wide suite failures as blocking if this feature's assigned reproducer can run independently; report unrelated failures separately and continue with the assigned bug reproducer. If the assigned reproducer already passes on the current branch before any edits, do not manufacture a failure; instead collect proof-of-already-fixed-on-branch, run an adjacent confidence check, inspect the relevant code path, and close the feature with evidence.

- Milestone: `section4-compiler-pipeline`
- Skill: `bug-fix-worker`
- Fulfills: VAL-CP-005
- Status: `pending`

## 39. section4-compiler-pipeline-bug-cp-006

Fix audited bug cp-006 in Compiler Pipeline. Reproduce the exact failure first, add or update a failing targeted test or reproducer, implement the smallest fix for this one bug only, run focused validators, and confirm the original reproduction no longer fails. If live environment prerequisites for this bug are blocked (for example VPS or local OAuth proxy), reproduce the blocker explicitly and return with blocker evidence instead of claiming success. Do not treat unrelated mission-wide suite failures as blocking if this feature's assigned reproducer can run independently; report unrelated failures separately and continue with the assigned bug reproducer. If the assigned reproducer already passes on the current branch before any edits, do not manufacture a failure; instead collect proof-of-already-fixed-on-branch, run an adjacent confidence check, inspect the relevant code path, and close the feature with evidence.

- Milestone: `section4-compiler-pipeline`
- Skill: `bug-fix-worker`
- Fulfills: VAL-CP-006
- Status: `pending`

## 40. section4-compiler-pipeline-bug-cp-007

Fix audited bug cp-007 in Compiler Pipeline. Reproduce the exact failure first, add or update a failing targeted test or reproducer, implement the smallest fix for this one bug only, run focused validators, and confirm the original reproduction no longer fails. If live environment prerequisites for this bug are blocked (for example VPS or local OAuth proxy), reproduce the blocker explicitly and return with blocker evidence instead of claiming success. Do not treat unrelated mission-wide suite failures as blocking if this feature's assigned reproducer can run independently; report unrelated failures separately and continue with the assigned bug reproducer. If the assigned reproducer already passes on the current branch before any edits, do not manufacture a failure; instead collect proof-of-already-fixed-on-branch, run an adjacent confidence check, inspect the relevant code path, and close the feature with evidence.

- Milestone: `section4-compiler-pipeline`
- Skill: `bug-fix-worker`
- Fulfills: VAL-CP-007
- Status: `pending`

## 41. section4-compiler-pipeline-bug-cp-008

Fix audited bug cp-008 in Compiler Pipeline. Reproduce the exact failure first, add or update a failing targeted test or reproducer, implement the smallest fix for this one bug only, run focused validators, and confirm the original reproduction no longer fails. If live environment prerequisites for this bug are blocked (for example VPS or local OAuth proxy), reproduce the blocker explicitly and return with blocker evidence instead of claiming success. Do not treat unrelated mission-wide suite failures as blocking if this feature's assigned reproducer can run independently; report unrelated failures separately and continue with the assigned bug reproducer. If the assigned reproducer already passes on the current branch before any edits, do not manufacture a failure; instead collect proof-of-already-fixed-on-branch, run an adjacent confidence check, inspect the relevant code path, and close the feature with evidence.

- Milestone: `section4-compiler-pipeline`
- Skill: `bug-fix-worker`
- Fulfills: VAL-CP-008
- Status: `pending`

## 42. section4-compiler-pipeline-bug-cp-009

Fix audited bug cp-009 in Compiler Pipeline. Reproduce the exact failure first, add or update a failing targeted test or reproducer, implement the smallest fix for this one bug only, run focused validators, and confirm the original reproduction no longer fails. If live environment prerequisites for this bug are blocked (for example VPS or local OAuth proxy), reproduce the blocker explicitly and return with blocker evidence instead of claiming success. Do not treat unrelated mission-wide suite failures as blocking if this feature's assigned reproducer can run independently; report unrelated failures separately and continue with the assigned bug reproducer. If the assigned reproducer already passes on the current branch before any edits, do not manufacture a failure; instead collect proof-of-already-fixed-on-branch, run an adjacent confidence check, inspect the relevant code path, and close the feature with evidence.

- Milestone: `section4-compiler-pipeline`
- Skill: `bug-fix-worker`
- Fulfills: VAL-CP-009
- Status: `pending`

## 43. section4-compiler-pipeline-bug-cp-010

Fix audited bug cp-010 in Compiler Pipeline. Reproduce the exact failure first, add or update a failing targeted test or reproducer, implement the smallest fix for this one bug only, run focused validators, and confirm the original reproduction no longer fails. If live environment prerequisites for this bug are blocked (for example VPS or local OAuth proxy), reproduce the blocker explicitly and return with blocker evidence instead of claiming success. Do not treat unrelated mission-wide suite failures as blocking if this feature's assigned reproducer can run independently; report unrelated failures separately and continue with the assigned bug reproducer. If the assigned reproducer already passes on the current branch before any edits, do not manufacture a failure; instead collect proof-of-already-fixed-on-branch, run an adjacent confidence check, inspect the relevant code path, and close the feature with evidence.

- Milestone: `section4-compiler-pipeline`
- Skill: `bug-fix-worker`
- Fulfills: VAL-CP-010
- Status: `pending`

## 44. section4-compiler-pipeline-bug-cp-011

Fix audited bug cp-011 in Compiler Pipeline. Reproduce the exact failure first, add or update a failing targeted test or reproducer, implement the smallest fix for this one bug only, run focused validators, and confirm the original reproduction no longer fails. If live environment prerequisites for this bug are blocked (for example VPS or local OAuth proxy), reproduce the blocker explicitly and return with blocker evidence instead of claiming success. Do not treat unrelated mission-wide suite failures as blocking if this feature's assigned reproducer can run independently; report unrelated failures separately and continue with the assigned bug reproducer. If the assigned reproducer already passes on the current branch before any edits, do not manufacture a failure; instead collect proof-of-already-fixed-on-branch, run an adjacent confidence check, inspect the relevant code path, and close the feature with evidence.

- Milestone: `section4-compiler-pipeline`
- Skill: `bug-fix-worker`
- Fulfills: VAL-CP-011
- Status: `pending`

## 45. section4-compiler-pipeline-bug-cp-012

Fix audited bug cp-012 in Compiler Pipeline. Reproduce the exact failure first, add or update a failing targeted test or reproducer, implement the smallest fix for this one bug only, run focused validators, and confirm the original reproduction no longer fails. If live environment prerequisites for this bug are blocked (for example VPS or local OAuth proxy), reproduce the blocker explicitly and return with blocker evidence instead of claiming success. Do not treat unrelated mission-wide suite failures as blocking if this feature's assigned reproducer can run independently; report unrelated failures separately and continue with the assigned bug reproducer. If the assigned reproducer already passes on the current branch before any edits, do not manufacture a failure; instead collect proof-of-already-fixed-on-branch, run an adjacent confidence check, inspect the relevant code path, and close the feature with evidence.

- Milestone: `section4-compiler-pipeline`
- Skill: `bug-fix-worker`
- Fulfills: VAL-CP-012
- Status: `pending`

## 46. section4-compiler-pipeline-bug-cp-013

Fix audited bug cp-013 in Compiler Pipeline. Reproduce the exact failure first, add or update a failing targeted test or reproducer, implement the smallest fix for this one bug only, run focused validators, and confirm the original reproduction no longer fails. If live environment prerequisites for this bug are blocked (for example VPS or local OAuth proxy), reproduce the blocker explicitly and return with blocker evidence instead of claiming success. Do not treat unrelated mission-wide suite failures as blocking if this feature's assigned reproducer can run independently; report unrelated failures separately and continue with the assigned bug reproducer. If the assigned reproducer already passes on the current branch before any edits, do not manufacture a failure; instead collect proof-of-already-fixed-on-branch, run an adjacent confidence check, inspect the relevant code path, and close the feature with evidence.

- Milestone: `section4-compiler-pipeline`
- Skill: `bug-fix-worker`
- Fulfills: VAL-CP-013
- Status: `pending`

## 47. section4-compiler-pipeline-bug-cp-014

Fix audited bug cp-014 in Compiler Pipeline. Reproduce the exact failure first, add or update a failing targeted test or reproducer, implement the smallest fix for this one bug only, run focused validators, and confirm the original reproduction no longer fails. If live environment prerequisites for this bug are blocked (for example VPS or local OAuth proxy), reproduce the blocker explicitly and return with blocker evidence instead of claiming success. Do not treat unrelated mission-wide suite failures as blocking if this feature's assigned reproducer can run independently; report unrelated failures separately and continue with the assigned bug reproducer. If the assigned reproducer already passes on the current branch before any edits, do not manufacture a failure; instead collect proof-of-already-fixed-on-branch, run an adjacent confidence check, inspect the relevant code path, and close the feature with evidence.

- Milestone: `section4-compiler-pipeline`
- Skill: `bug-fix-worker`
- Fulfills: VAL-CP-014
- Status: `pending`

## 48. section4-compiler-pipeline-bug-cp-015

Fix audited bug cp-015 in Compiler Pipeline. Reproduce the exact failure first, add or update a failing targeted test or reproducer, implement the smallest fix for this one bug only, run focused validators, and confirm the original reproduction no longer fails. If live environment prerequisites for this bug are blocked (for example VPS or local OAuth proxy), reproduce the blocker explicitly and return with blocker evidence instead of claiming success. Do not treat unrelated mission-wide suite failures as blocking if this feature's assigned reproducer can run independently; report unrelated failures separately and continue with the assigned bug reproducer. If the assigned reproducer already passes on the current branch before any edits, do not manufacture a failure; instead collect proof-of-already-fixed-on-branch, run an adjacent confidence check, inspect the relevant code path, and close the feature with evidence.

- Milestone: `section4-compiler-pipeline`
- Skill: `bug-fix-worker`
- Fulfills: VAL-CP-015
- Status: `pending`

## 49. section4-compiler-pipeline-bug-cp-016

Fix audited bug cp-016 in Compiler Pipeline. Reproduce the exact failure first, add or update a failing targeted test or reproducer, implement the smallest fix for this one bug only, run focused validators, and confirm the original reproduction no longer fails. If live environment prerequisites for this bug are blocked (for example VPS or local OAuth proxy), reproduce the blocker explicitly and return with blocker evidence instead of claiming success. Do not treat unrelated mission-wide suite failures as blocking if this feature's assigned reproducer can run independently; report unrelated failures separately and continue with the assigned bug reproducer. If the assigned reproducer already passes on the current branch before any edits, do not manufacture a failure; instead collect proof-of-already-fixed-on-branch, run an adjacent confidence check, inspect the relevant code path, and close the feature with evidence.

- Milestone: `section4-compiler-pipeline`
- Skill: `bug-fix-worker`
- Fulfills: VAL-CP-016
- Status: `pending`

## 50. section4-compiler-pipeline-bug-cp-017

Fix audited bug cp-017 in Compiler Pipeline. Reproduce the exact failure first, add or update a failing targeted test or reproducer, implement the smallest fix for this one bug only, run focused validators, and confirm the original reproduction no longer fails. If live environment prerequisites for this bug are blocked (for example VPS or local OAuth proxy), reproduce the blocker explicitly and return with blocker evidence instead of claiming success. Do not treat unrelated mission-wide suite failures as blocking if this feature's assigned reproducer can run independently; report unrelated failures separately and continue with the assigned bug reproducer. If the assigned reproducer already passes on the current branch before any edits, do not manufacture a failure; instead collect proof-of-already-fixed-on-branch, run an adjacent confidence check, inspect the relevant code path, and close the feature with evidence.

- Milestone: `section4-compiler-pipeline`
- Skill: `bug-fix-worker`
- Fulfills: VAL-CP-017
- Status: `pending`

## 51. section5-storage-data-bug-sd-001

Fix audited bug sd-001 in Storage / Data. Reproduce the exact failure first, add or update a failing targeted test or reproducer, implement the smallest fix for this one bug only, run focused validators, and confirm the original reproduction no longer fails. If live environment prerequisites for this bug are blocked (for example VPS or local OAuth proxy), reproduce the blocker explicitly and return with blocker evidence instead of claiming success. Do not treat unrelated mission-wide suite failures as blocking if this feature's assigned reproducer can run independently; report unrelated failures separately and continue with the assigned bug reproducer. If the assigned reproducer already passes on the current branch before any edits, do not manufacture a failure; instead collect proof-of-already-fixed-on-branch, run an adjacent confidence check, inspect the relevant code path, and close the feature with evidence.

- Milestone: `section5-storage-data`
- Skill: `bug-fix-worker`
- Fulfills: VAL-SD-001
- Status: `pending`

## 52. section5-storage-data-bug-sd-002

Fix audited bug sd-002 in Storage / Data. Reproduce the exact failure first, add or update a failing targeted test or reproducer, implement the smallest fix for this one bug only, run focused validators, and confirm the original reproduction no longer fails. If live environment prerequisites for this bug are blocked (for example VPS or local OAuth proxy), reproduce the blocker explicitly and return with blocker evidence instead of claiming success. Do not treat unrelated mission-wide suite failures as blocking if this feature's assigned reproducer can run independently; report unrelated failures separately and continue with the assigned bug reproducer. If the assigned reproducer already passes on the current branch before any edits, do not manufacture a failure; instead collect proof-of-already-fixed-on-branch, run an adjacent confidence check, inspect the relevant code path, and close the feature with evidence.

- Milestone: `section5-storage-data`
- Skill: `bug-fix-worker`
- Fulfills: VAL-SD-002
- Status: `pending`

## 53. section5-storage-data-bug-sd-003

Fix audited bug sd-003 in Storage / Data. Reproduce the exact failure first, add or update a failing targeted test or reproducer, implement the smallest fix for this one bug only, run focused validators, and confirm the original reproduction no longer fails. If live environment prerequisites for this bug are blocked (for example VPS or local OAuth proxy), reproduce the blocker explicitly and return with blocker evidence instead of claiming success. Do not treat unrelated mission-wide suite failures as blocking if this feature's assigned reproducer can run independently; report unrelated failures separately and continue with the assigned bug reproducer. If the assigned reproducer already passes on the current branch before any edits, do not manufacture a failure; instead collect proof-of-already-fixed-on-branch, run an adjacent confidence check, inspect the relevant code path, and close the feature with evidence.

- Milestone: `section5-storage-data`
- Skill: `bug-fix-worker`
- Fulfills: VAL-SD-003
- Status: `pending`

## 54. section5-storage-data-bug-sd-004

Fix audited bug sd-004 in Storage / Data. Reproduce the exact failure first, add or update a failing targeted test or reproducer, implement the smallest fix for this one bug only, run focused validators, and confirm the original reproduction no longer fails. If live environment prerequisites for this bug are blocked (for example VPS or local OAuth proxy), reproduce the blocker explicitly and return with blocker evidence instead of claiming success. Do not treat unrelated mission-wide suite failures as blocking if this feature's assigned reproducer can run independently; report unrelated failures separately and continue with the assigned bug reproducer. If the assigned reproducer already passes on the current branch before any edits, do not manufacture a failure; instead collect proof-of-already-fixed-on-branch, run an adjacent confidence check, inspect the relevant code path, and close the feature with evidence.

- Milestone: `section5-storage-data`
- Skill: `bug-fix-worker`
- Fulfills: VAL-SD-004
- Status: `pending`

## 55. section5-storage-data-bug-sd-005

Fix audited bug sd-005 in Storage / Data. Reproduce the exact failure first, add or update a failing targeted test or reproducer, implement the smallest fix for this one bug only, run focused validators, and confirm the original reproduction no longer fails. If live environment prerequisites for this bug are blocked (for example VPS or local OAuth proxy), reproduce the blocker explicitly and return with blocker evidence instead of claiming success. Do not treat unrelated mission-wide suite failures as blocking if this feature's assigned reproducer can run independently; report unrelated failures separately and continue with the assigned bug reproducer. If the assigned reproducer already passes on the current branch before any edits, do not manufacture a failure; instead collect proof-of-already-fixed-on-branch, run an adjacent confidence check, inspect the relevant code path, and close the feature with evidence.

- Milestone: `section5-storage-data`
- Skill: `bug-fix-worker`
- Fulfills: VAL-SD-005
- Status: `pending`

## 56. section5-storage-data-bug-sd-006

Fix audited bug sd-006 in Storage / Data. Reproduce the exact failure first, add or update a failing targeted test or reproducer, implement the smallest fix for this one bug only, run focused validators, and confirm the original reproduction no longer fails. If live environment prerequisites for this bug are blocked (for example VPS or local OAuth proxy), reproduce the blocker explicitly and return with blocker evidence instead of claiming success. Do not treat unrelated mission-wide suite failures as blocking if this feature's assigned reproducer can run independently; report unrelated failures separately and continue with the assigned bug reproducer. If the assigned reproducer already passes on the current branch before any edits, do not manufacture a failure; instead collect proof-of-already-fixed-on-branch, run an adjacent confidence check, inspect the relevant code path, and close the feature with evidence.

- Milestone: `section5-storage-data`
- Skill: `bug-fix-worker`
- Fulfills: VAL-SD-006
- Status: `pending`

## 57. section5-storage-data-bug-sd-007

Fix audited bug sd-007 in Storage / Data. Reproduce the exact failure first, add or update a failing targeted test or reproducer, implement the smallest fix for this one bug only, run focused validators, and confirm the original reproduction no longer fails. If live environment prerequisites for this bug are blocked (for example VPS or local OAuth proxy), reproduce the blocker explicitly and return with blocker evidence instead of claiming success. Do not treat unrelated mission-wide suite failures as blocking if this feature's assigned reproducer can run independently; report unrelated failures separately and continue with the assigned bug reproducer. If the assigned reproducer already passes on the current branch before any edits, do not manufacture a failure; instead collect proof-of-already-fixed-on-branch, run an adjacent confidence check, inspect the relevant code path, and close the feature with evidence.

- Milestone: `section5-storage-data`
- Skill: `bug-fix-worker`
- Fulfills: VAL-SD-007
- Status: `pending`

## 58. section5-storage-data-bug-sd-008

Fix audited bug sd-008 in Storage / Data. Reproduce the exact failure first, add or update a failing targeted test or reproducer, implement the smallest fix for this one bug only, run focused validators, and confirm the original reproduction no longer fails. If live environment prerequisites for this bug are blocked (for example VPS or local OAuth proxy), reproduce the blocker explicitly and return with blocker evidence instead of claiming success. Do not treat unrelated mission-wide suite failures as blocking if this feature's assigned reproducer can run independently; report unrelated failures separately and continue with the assigned bug reproducer. If the assigned reproducer already passes on the current branch before any edits, do not manufacture a failure; instead collect proof-of-already-fixed-on-branch, run an adjacent confidence check, inspect the relevant code path, and close the feature with evidence.

- Milestone: `section5-storage-data`
- Skill: `bug-fix-worker`
- Fulfills: VAL-SD-008
- Status: `pending`

## 59. section5-storage-data-bug-sd-009

Fix audited bug sd-009 in Storage / Data. Reproduce the exact failure first, add or update a failing targeted test or reproducer, implement the smallest fix for this one bug only, run focused validators, and confirm the original reproduction no longer fails. If live environment prerequisites for this bug are blocked (for example VPS or local OAuth proxy), reproduce the blocker explicitly and return with blocker evidence instead of claiming success. Do not treat unrelated mission-wide suite failures as blocking if this feature's assigned reproducer can run independently; report unrelated failures separately and continue with the assigned bug reproducer. If the assigned reproducer already passes on the current branch before any edits, do not manufacture a failure; instead collect proof-of-already-fixed-on-branch, run an adjacent confidence check, inspect the relevant code path, and close the feature with evidence.

- Milestone: `section5-storage-data`
- Skill: `bug-fix-worker`
- Fulfills: VAL-SD-009
- Status: `pending`

## 60. section5-storage-data-bug-sd-010

Fix audited bug sd-010 in Storage / Data. Reproduce the exact failure first, add or update a failing targeted test or reproducer, implement the smallest fix for this one bug only, run focused validators, and confirm the original reproduction no longer fails. If live environment prerequisites for this bug are blocked (for example VPS or local OAuth proxy), reproduce the blocker explicitly and return with blocker evidence instead of claiming success. Do not treat unrelated mission-wide suite failures as blocking if this feature's assigned reproducer can run independently; report unrelated failures separately and continue with the assigned bug reproducer. If the assigned reproducer already passes on the current branch before any edits, do not manufacture a failure; instead collect proof-of-already-fixed-on-branch, run an adjacent confidence check, inspect the relevant code path, and close the feature with evidence.

- Milestone: `section5-storage-data`
- Skill: `bug-fix-worker`
- Fulfills: VAL-SD-010
- Status: `pending`

## 61. section5-storage-data-bug-sd-011

Fix audited bug sd-011 in Storage / Data. Reproduce the exact failure first, add or update a failing targeted test or reproducer, implement the smallest fix for this one bug only, run focused validators, and confirm the original reproduction no longer fails. If live environment prerequisites for this bug are blocked (for example VPS or local OAuth proxy), reproduce the blocker explicitly and return with blocker evidence instead of claiming success. Do not treat unrelated mission-wide suite failures as blocking if this feature's assigned reproducer can run independently; report unrelated failures separately and continue with the assigned bug reproducer. If the assigned reproducer already passes on the current branch before any edits, do not manufacture a failure; instead collect proof-of-already-fixed-on-branch, run an adjacent confidence check, inspect the relevant code path, and close the feature with evidence.

- Milestone: `section5-storage-data`
- Skill: `bug-fix-worker`
- Fulfills: VAL-SD-011
- Status: `pending`

## 62. section5-storage-data-bug-sd-012

Fix audited bug sd-012 in Storage / Data. Reproduce the exact failure first, add or update a failing targeted test or reproducer, implement the smallest fix for this one bug only, run focused validators, and confirm the original reproduction no longer fails. If live environment prerequisites for this bug are blocked (for example VPS or local OAuth proxy), reproduce the blocker explicitly and return with blocker evidence instead of claiming success. Do not treat unrelated mission-wide suite failures as blocking if this feature's assigned reproducer can run independently; report unrelated failures separately and continue with the assigned bug reproducer. If the assigned reproducer already passes on the current branch before any edits, do not manufacture a failure; instead collect proof-of-already-fixed-on-branch, run an adjacent confidence check, inspect the relevant code path, and close the feature with evidence.

- Milestone: `section5-storage-data`
- Skill: `bug-fix-worker`
- Fulfills: VAL-SD-012
- Status: `pending`

## 63. section6-observability-bug-obs-001

Fix audited bug obs-001 in Observability. Reproduce the exact failure first, add or update a failing targeted test or reproducer, implement the smallest fix for this one bug only, run focused validators, and confirm the original reproduction no longer fails. If live environment prerequisites for this bug are blocked (for example VPS or local OAuth proxy), reproduce the blocker explicitly and return with blocker evidence instead of claiming success. Do not treat unrelated mission-wide suite failures as blocking if this feature's assigned reproducer can run independently; report unrelated failures separately and continue with the assigned bug reproducer. If the assigned reproducer already passes on the current branch before any edits, do not manufacture a failure; instead collect proof-of-already-fixed-on-branch, run an adjacent confidence check, inspect the relevant code path, and close the feature with evidence.

- Milestone: `section6-observability`
- Skill: `bug-fix-worker`
- Fulfills: VAL-OBS-001
- Status: `pending`

## 64. section6-observability-bug-obs-002

Fix audited bug obs-002 in Observability. Reproduce the exact failure first, add or update a failing targeted test or reproducer, implement the smallest fix for this one bug only, run focused validators, and confirm the original reproduction no longer fails. If live environment prerequisites for this bug are blocked (for example VPS or local OAuth proxy), reproduce the blocker explicitly and return with blocker evidence instead of claiming success. Do not treat unrelated mission-wide suite failures as blocking if this feature's assigned reproducer can run independently; report unrelated failures separately and continue with the assigned bug reproducer. If the assigned reproducer already passes on the current branch before any edits, do not manufacture a failure; instead collect proof-of-already-fixed-on-branch, run an adjacent confidence check, inspect the relevant code path, and close the feature with evidence.

- Milestone: `section6-observability`
- Skill: `bug-fix-worker`
- Fulfills: VAL-OBS-002
- Status: `pending`

## 65. section6-observability-bug-obs-003

Fix audited bug obs-003 in Observability. Reproduce the exact failure first, add or update a failing targeted test or reproducer, implement the smallest fix for this one bug only, run focused validators, and confirm the original reproduction no longer fails. If live environment prerequisites for this bug are blocked (for example VPS or local OAuth proxy), reproduce the blocker explicitly and return with blocker evidence instead of claiming success. Do not treat unrelated mission-wide suite failures as blocking if this feature's assigned reproducer can run independently; report unrelated failures separately and continue with the assigned bug reproducer. If the assigned reproducer already passes on the current branch before any edits, do not manufacture a failure; instead collect proof-of-already-fixed-on-branch, run an adjacent confidence check, inspect the relevant code path, and close the feature with evidence.

- Milestone: `section6-observability`
- Skill: `bug-fix-worker`
- Fulfills: VAL-OBS-003
- Status: `pending`

## 66. section6-observability-bug-obs-004

Fix audited bug obs-004 in Observability. Reproduce the exact failure first, add or update a failing targeted test or reproducer, implement the smallest fix for this one bug only, run focused validators, and confirm the original reproduction no longer fails. If live environment prerequisites for this bug are blocked (for example VPS or local OAuth proxy), reproduce the blocker explicitly and return with blocker evidence instead of claiming success. Do not treat unrelated mission-wide suite failures as blocking if this feature's assigned reproducer can run independently; report unrelated failures separately and continue with the assigned bug reproducer. If the assigned reproducer already passes on the current branch before any edits, do not manufacture a failure; instead collect proof-of-already-fixed-on-branch, run an adjacent confidence check, inspect the relevant code path, and close the feature with evidence.

- Milestone: `section6-observability`
- Skill: `bug-fix-worker`
- Fulfills: VAL-OBS-004
- Status: `pending`

## 67. section6-observability-bug-obs-005

Fix audited bug obs-005 in Observability. Reproduce the exact failure first, add or update a failing targeted test or reproducer, implement the smallest fix for this one bug only, run focused validators, and confirm the original reproduction no longer fails. If live environment prerequisites for this bug are blocked (for example VPS or local OAuth proxy), reproduce the blocker explicitly and return with blocker evidence instead of claiming success. Do not treat unrelated mission-wide suite failures as blocking if this feature's assigned reproducer can run independently; report unrelated failures separately and continue with the assigned bug reproducer. If the assigned reproducer already passes on the current branch before any edits, do not manufacture a failure; instead collect proof-of-already-fixed-on-branch, run an adjacent confidence check, inspect the relevant code path, and close the feature with evidence.

- Milestone: `section6-observability`
- Skill: `bug-fix-worker`
- Fulfills: VAL-OBS-005
- Status: `pending`

## 68. section6-observability-bug-obs-006

Fix audited bug obs-006 in Observability. Reproduce the exact failure first, add or update a failing targeted test or reproducer, implement the smallest fix for this one bug only, run focused validators, and confirm the original reproduction no longer fails. If live environment prerequisites for this bug are blocked (for example VPS or local OAuth proxy), reproduce the blocker explicitly and return with blocker evidence instead of claiming success. Do not treat unrelated mission-wide suite failures as blocking if this feature's assigned reproducer can run independently; report unrelated failures separately and continue with the assigned bug reproducer. If the assigned reproducer already passes on the current branch before any edits, do not manufacture a failure; instead collect proof-of-already-fixed-on-branch, run an adjacent confidence check, inspect the relevant code path, and close the feature with evidence.

- Milestone: `section6-observability`
- Skill: `bug-fix-worker`
- Fulfills: VAL-OBS-006
- Status: `pending`

## 69. section6-observability-bug-obs-007

Fix audited bug obs-007 in Observability. Reproduce the exact failure first, add or update a failing targeted test or reproducer, implement the smallest fix for this one bug only, run focused validators, and confirm the original reproduction no longer fails. If live environment prerequisites for this bug are blocked (for example VPS or local OAuth proxy), reproduce the blocker explicitly and return with blocker evidence instead of claiming success. Do not treat unrelated mission-wide suite failures as blocking if this feature's assigned reproducer can run independently; report unrelated failures separately and continue with the assigned bug reproducer. If the assigned reproducer already passes on the current branch before any edits, do not manufacture a failure; instead collect proof-of-already-fixed-on-branch, run an adjacent confidence check, inspect the relevant code path, and close the feature with evidence.

- Milestone: `section6-observability`
- Skill: `bug-fix-worker`
- Fulfills: VAL-OBS-007
- Status: `pending`

## 70. section6-observability-bug-obs-008

Fix audited bug obs-008 in Observability. Reproduce the exact failure first, add or update a failing targeted test or reproducer, implement the smallest fix for this one bug only, run focused validators, and confirm the original reproduction no longer fails. If live environment prerequisites for this bug are blocked (for example VPS or local OAuth proxy), reproduce the blocker explicitly and return with blocker evidence instead of claiming success. Do not treat unrelated mission-wide suite failures as blocking if this feature's assigned reproducer can run independently; report unrelated failures separately and continue with the assigned bug reproducer. If the assigned reproducer already passes on the current branch before any edits, do not manufacture a failure; instead collect proof-of-already-fixed-on-branch, run an adjacent confidence check, inspect the relevant code path, and close the feature with evidence.

- Milestone: `section6-observability`
- Skill: `bug-fix-worker`
- Fulfills: VAL-OBS-008
- Status: `pending`

## 71. section6-observability-bug-obs-009

Fix audited bug obs-009 in Observability. Reproduce the exact failure first, add or update a failing targeted test or reproducer, implement the smallest fix for this one bug only, run focused validators, and confirm the original reproduction no longer fails. If live environment prerequisites for this bug are blocked (for example VPS or local OAuth proxy), reproduce the blocker explicitly and return with blocker evidence instead of claiming success. Do not treat unrelated mission-wide suite failures as blocking if this feature's assigned reproducer can run independently; report unrelated failures separately and continue with the assigned bug reproducer. If the assigned reproducer already passes on the current branch before any edits, do not manufacture a failure; instead collect proof-of-already-fixed-on-branch, run an adjacent confidence check, inspect the relevant code path, and close the feature with evidence.

- Milestone: `section6-observability`
- Skill: `bug-fix-worker`
- Fulfills: VAL-OBS-009
- Status: `pending`

## 72. section6-observability-bug-obs-010

Fix audited bug obs-010 in Observability. Reproduce the exact failure first, add or update a failing targeted test or reproducer, implement the smallest fix for this one bug only, run focused validators, and confirm the original reproduction no longer fails. If live environment prerequisites for this bug are blocked (for example VPS or local OAuth proxy), reproduce the blocker explicitly and return with blocker evidence instead of claiming success. Do not treat unrelated mission-wide suite failures as blocking if this feature's assigned reproducer can run independently; report unrelated failures separately and continue with the assigned bug reproducer. If the assigned reproducer already passes on the current branch before any edits, do not manufacture a failure; instead collect proof-of-already-fixed-on-branch, run an adjacent confidence check, inspect the relevant code path, and close the feature with evidence.

- Milestone: `section6-observability`
- Skill: `bug-fix-worker`
- Fulfills: VAL-OBS-010
- Status: `pending`

## 73. section6-observability-bug-obs-011

Fix audited bug obs-011 in Observability. Reproduce the exact failure first, add or update a failing targeted test or reproducer, implement the smallest fix for this one bug only, run focused validators, and confirm the original reproduction no longer fails. If live environment prerequisites for this bug are blocked (for example VPS or local OAuth proxy), reproduce the blocker explicitly and return with blocker evidence instead of claiming success. Do not treat unrelated mission-wide suite failures as blocking if this feature's assigned reproducer can run independently; report unrelated failures separately and continue with the assigned bug reproducer. If the assigned reproducer already passes on the current branch before any edits, do not manufacture a failure; instead collect proof-of-already-fixed-on-branch, run an adjacent confidence check, inspect the relevant code path, and close the feature with evidence.

- Milestone: `section6-observability`
- Skill: `bug-fix-worker`
- Fulfills: VAL-OBS-011
- Status: `pending`

## 74. section6-observability-bug-obs-012

Fix audited bug obs-012 in Observability. Reproduce the exact failure first, add or update a failing targeted test or reproducer, implement the smallest fix for this one bug only, run focused validators, and confirm the original reproduction no longer fails. If live environment prerequisites for this bug are blocked (for example VPS or local OAuth proxy), reproduce the blocker explicitly and return with blocker evidence instead of claiming success. Do not treat unrelated mission-wide suite failures as blocking if this feature's assigned reproducer can run independently; report unrelated failures separately and continue with the assigned bug reproducer. If the assigned reproducer already passes on the current branch before any edits, do not manufacture a failure; instead collect proof-of-already-fixed-on-branch, run an adjacent confidence check, inspect the relevant code path, and close the feature with evidence.

- Milestone: `section6-observability`
- Skill: `bug-fix-worker`
- Fulfills: VAL-OBS-012
- Status: `pending`

## 75. section6-observability-bug-obs-013

Fix audited bug obs-013 in Observability. Reproduce the exact failure first, add or update a failing targeted test or reproducer, implement the smallest fix for this one bug only, run focused validators, and confirm the original reproduction no longer fails. If live environment prerequisites for this bug are blocked (for example VPS or local OAuth proxy), reproduce the blocker explicitly and return with blocker evidence instead of claiming success. Do not treat unrelated mission-wide suite failures as blocking if this feature's assigned reproducer can run independently; report unrelated failures separately and continue with the assigned bug reproducer. If the assigned reproducer already passes on the current branch before any edits, do not manufacture a failure; instead collect proof-of-already-fixed-on-branch, run an adjacent confidence check, inspect the relevant code path, and close the feature with evidence.

- Milestone: `section6-observability`
- Skill: `bug-fix-worker`
- Fulfills: VAL-OBS-013
- Status: `pending`

## 76. section6-observability-bug-obs-014

Fix audited bug obs-014 in Observability. Reproduce the exact failure first, add or update a failing targeted test or reproducer, implement the smallest fix for this one bug only, run focused validators, and confirm the original reproduction no longer fails. If live environment prerequisites for this bug are blocked (for example VPS or local OAuth proxy), reproduce the blocker explicitly and return with blocker evidence instead of claiming success. Do not treat unrelated mission-wide suite failures as blocking if this feature's assigned reproducer can run independently; report unrelated failures separately and continue with the assigned bug reproducer. If the assigned reproducer already passes on the current branch before any edits, do not manufacture a failure; instead collect proof-of-already-fixed-on-branch, run an adjacent confidence check, inspect the relevant code path, and close the feature with evidence.

- Milestone: `section6-observability`
- Skill: `bug-fix-worker`
- Fulfills: VAL-OBS-014
- Status: `pending`

## 77. section6-observability-bug-obs-015

Fix audited bug obs-015 in Observability. Reproduce the exact failure first, add or update a failing targeted test or reproducer, implement the smallest fix for this one bug only, run focused validators, and confirm the original reproduction no longer fails. If live environment prerequisites for this bug are blocked (for example VPS or local OAuth proxy), reproduce the blocker explicitly and return with blocker evidence instead of claiming success. Do not treat unrelated mission-wide suite failures as blocking if this feature's assigned reproducer can run independently; report unrelated failures separately and continue with the assigned bug reproducer. If the assigned reproducer already passes on the current branch before any edits, do not manufacture a failure; instead collect proof-of-already-fixed-on-branch, run an adjacent confidence check, inspect the relevant code path, and close the feature with evidence.

- Milestone: `section6-observability`
- Skill: `bug-fix-worker`
- Fulfills: VAL-OBS-015
- Status: `pending`

## 78. section7-deployment-vps-bug-vps-001

Fix audited bug vps-001 in Deployment / VPS. Reproduce the exact failure first, add or update a failing targeted test or reproducer, implement the smallest fix for this one bug only, run focused validators, and confirm the original reproduction no longer fails. If live environment prerequisites for this bug are blocked (for example VPS or local OAuth proxy), reproduce the blocker explicitly and return with blocker evidence instead of claiming success. Do not treat unrelated mission-wide suite failures as blocking if this feature's assigned reproducer can run independently; report unrelated failures separately and continue with the assigned bug reproducer. If the assigned reproducer already passes on the current branch before any edits, do not manufacture a failure; instead collect proof-of-already-fixed-on-branch, run an adjacent confidence check, inspect the relevant code path, and close the feature with evidence.

- Milestone: `section7-deployment-vps`
- Skill: `bug-fix-worker`
- Fulfills: VAL-VPS-001
- Status: `pending`

## 79. section7-deployment-vps-bug-vps-002

Fix audited bug vps-002 in Deployment / VPS. Reproduce the exact failure first, add or update a failing targeted test or reproducer, implement the smallest fix for this one bug only, run focused validators, and confirm the original reproduction no longer fails. If live environment prerequisites for this bug are blocked (for example VPS or local OAuth proxy), reproduce the blocker explicitly and return with blocker evidence instead of claiming success. Do not treat unrelated mission-wide suite failures as blocking if this feature's assigned reproducer can run independently; report unrelated failures separately and continue with the assigned bug reproducer. If the assigned reproducer already passes on the current branch before any edits, do not manufacture a failure; instead collect proof-of-already-fixed-on-branch, run an adjacent confidence check, inspect the relevant code path, and close the feature with evidence.

- Milestone: `section7-deployment-vps`
- Skill: `bug-fix-worker`
- Fulfills: VAL-VPS-002
- Status: `pending`

## 80. section7-deployment-vps-bug-vps-003

Fix audited bug vps-003 in Deployment / VPS. Reproduce the exact failure first, add or update a failing targeted test or reproducer, implement the smallest fix for this one bug only, run focused validators, and confirm the original reproduction no longer fails. If live environment prerequisites for this bug are blocked (for example VPS or local OAuth proxy), reproduce the blocker explicitly and return with blocker evidence instead of claiming success. Do not treat unrelated mission-wide suite failures as blocking if this feature's assigned reproducer can run independently; report unrelated failures separately and continue with the assigned bug reproducer. If the assigned reproducer already passes on the current branch before any edits, do not manufacture a failure; instead collect proof-of-already-fixed-on-branch, run an adjacent confidence check, inspect the relevant code path, and close the feature with evidence.

- Milestone: `section7-deployment-vps`
- Skill: `bug-fix-worker`
- Fulfills: VAL-VPS-003
- Status: `pending`

## 81. section7-deployment-vps-bug-vps-004

Fix audited bug vps-004 in Deployment / VPS. Reproduce the exact failure first, add or update a failing targeted test or reproducer, implement the smallest fix for this one bug only, run focused validators, and confirm the original reproduction no longer fails. If live environment prerequisites for this bug are blocked (for example VPS or local OAuth proxy), reproduce the blocker explicitly and return with blocker evidence instead of claiming success. Do not treat unrelated mission-wide suite failures as blocking if this feature's assigned reproducer can run independently; report unrelated failures separately and continue with the assigned bug reproducer. If the assigned reproducer already passes on the current branch before any edits, do not manufacture a failure; instead collect proof-of-already-fixed-on-branch, run an adjacent confidence check, inspect the relevant code path, and close the feature with evidence.

- Milestone: `section7-deployment-vps`
- Skill: `bug-fix-worker`
- Fulfills: VAL-VPS-004
- Status: `pending`

## 82. section7-deployment-vps-bug-vps-005

Fix audited bug vps-005 in Deployment / VPS. Reproduce the exact failure first, add or update a failing targeted test or reproducer, implement the smallest fix for this one bug only, run focused validators, and confirm the original reproduction no longer fails. If live environment prerequisites for this bug are blocked (for example VPS or local OAuth proxy), reproduce the blocker explicitly and return with blocker evidence instead of claiming success. Do not treat unrelated mission-wide suite failures as blocking if this feature's assigned reproducer can run independently; report unrelated failures separately and continue with the assigned bug reproducer. If the assigned reproducer already passes on the current branch before any edits, do not manufacture a failure; instead collect proof-of-already-fixed-on-branch, run an adjacent confidence check, inspect the relevant code path, and close the feature with evidence.

- Milestone: `section7-deployment-vps`
- Skill: `bug-fix-worker`
- Fulfills: VAL-VPS-005
- Status: `pending`

## 83. section7-deployment-vps-bug-vps-006

Fix audited bug vps-006 in Deployment / VPS. Reproduce the exact failure first, add or update a failing targeted test or reproducer, implement the smallest fix for this one bug only, run focused validators, and confirm the original reproduction no longer fails. If live environment prerequisites for this bug are blocked (for example VPS or local OAuth proxy), reproduce the blocker explicitly and return with blocker evidence instead of claiming success. Do not treat unrelated mission-wide suite failures as blocking if this feature's assigned reproducer can run independently; report unrelated failures separately and continue with the assigned bug reproducer. If the assigned reproducer already passes on the current branch before any edits, do not manufacture a failure; instead collect proof-of-already-fixed-on-branch, run an adjacent confidence check, inspect the relevant code path, and close the feature with evidence.

- Milestone: `section7-deployment-vps`
- Skill: `bug-fix-worker`
- Fulfills: VAL-VPS-006
- Status: `pending`

## 84. section7-deployment-vps-bug-vps-007

Fix audited bug vps-007 in Deployment / VPS. Reproduce the exact failure first, add or update a failing targeted test or reproducer, implement the smallest fix for this one bug only, run focused validators, and confirm the original reproduction no longer fails. If live environment prerequisites for this bug are blocked (for example VPS or local OAuth proxy), reproduce the blocker explicitly and return with blocker evidence instead of claiming success. Do not treat unrelated mission-wide suite failures as blocking if this feature's assigned reproducer can run independently; report unrelated failures separately and continue with the assigned bug reproducer. If the assigned reproducer already passes on the current branch before any edits, do not manufacture a failure; instead collect proof-of-already-fixed-on-branch, run an adjacent confidence check, inspect the relevant code path, and close the feature with evidence.

- Milestone: `section7-deployment-vps`
- Skill: `bug-fix-worker`
- Fulfills: VAL-VPS-007
- Status: `pending`

## 85. section7-deployment-vps-bug-vps-008

Fix audited bug vps-008 in Deployment / VPS. Reproduce the exact failure first, add or update a failing targeted test or reproducer, implement the smallest fix for this one bug only, run focused validators, and confirm the original reproduction no longer fails. If live environment prerequisites for this bug are blocked (for example VPS or local OAuth proxy), reproduce the blocker explicitly and return with blocker evidence instead of claiming success. Do not treat unrelated mission-wide suite failures as blocking if this feature's assigned reproducer can run independently; report unrelated failures separately and continue with the assigned bug reproducer. If the assigned reproducer already passes on the current branch before any edits, do not manufacture a failure; instead collect proof-of-already-fixed-on-branch, run an adjacent confidence check, inspect the relevant code path, and close the feature with evidence.

- Milestone: `section7-deployment-vps`
- Skill: `bug-fix-worker`
- Fulfills: VAL-VPS-008
- Status: `pending`

## 86. section7-deployment-vps-bug-vps-009

Fix audited bug vps-009 in Deployment / VPS. Reproduce the exact failure first, add or update a failing targeted test or reproducer, implement the smallest fix for this one bug only, run focused validators, and confirm the original reproduction no longer fails. If live environment prerequisites for this bug are blocked (for example VPS or local OAuth proxy), reproduce the blocker explicitly and return with blocker evidence instead of claiming success. Do not treat unrelated mission-wide suite failures as blocking if this feature's assigned reproducer can run independently; report unrelated failures separately and continue with the assigned bug reproducer. If the assigned reproducer already passes on the current branch before any edits, do not manufacture a failure; instead collect proof-of-already-fixed-on-branch, run an adjacent confidence check, inspect the relevant code path, and close the feature with evidence.

- Milestone: `section7-deployment-vps`
- Skill: `bug-fix-worker`
- Fulfills: VAL-VPS-009
- Status: `pending`

## 87. section7-deployment-vps-bug-vps-010

Fix audited bug vps-010 in Deployment / VPS. Reproduce the exact failure first, add or update a failing targeted test or reproducer, implement the smallest fix for this one bug only, run focused validators, and confirm the original reproduction no longer fails. If live environment prerequisites for this bug are blocked (for example VPS or local OAuth proxy), reproduce the blocker explicitly and return with blocker evidence instead of claiming success. Do not treat unrelated mission-wide suite failures as blocking if this feature's assigned reproducer can run independently; report unrelated failures separately and continue with the assigned bug reproducer. If the assigned reproducer already passes on the current branch before any edits, do not manufacture a failure; instead collect proof-of-already-fixed-on-branch, run an adjacent confidence check, inspect the relevant code path, and close the feature with evidence.

- Milestone: `section7-deployment-vps`
- Skill: `bug-fix-worker`
- Fulfills: VAL-VPS-010
- Status: `pending`

## 88. section7-deployment-vps-bug-vps-011

Fix audited bug vps-011 in Deployment / VPS. Reproduce the exact failure first, add or update a failing targeted test or reproducer, implement the smallest fix for this one bug only, run focused validators, and confirm the original reproduction no longer fails. If live environment prerequisites for this bug are blocked (for example VPS or local OAuth proxy), reproduce the blocker explicitly and return with blocker evidence instead of claiming success. Do not treat unrelated mission-wide suite failures as blocking if this feature's assigned reproducer can run independently; report unrelated failures separately and continue with the assigned bug reproducer. If the assigned reproducer already passes on the current branch before any edits, do not manufacture a failure; instead collect proof-of-already-fixed-on-branch, run an adjacent confidence check, inspect the relevant code path, and close the feature with evidence.

- Milestone: `section7-deployment-vps`
- Skill: `bug-fix-worker`
- Fulfills: VAL-VPS-011
- Status: `pending`

## 89. section7-deployment-vps-bug-vps-012

Fix audited bug vps-012 in Deployment / VPS. Reproduce the exact failure first, add or update a failing targeted test or reproducer, implement the smallest fix for this one bug only, run focused validators, and confirm the original reproduction no longer fails. If live environment prerequisites for this bug are blocked (for example VPS or local OAuth proxy), reproduce the blocker explicitly and return with blocker evidence instead of claiming success. Do not treat unrelated mission-wide suite failures as blocking if this feature's assigned reproducer can run independently; report unrelated failures separately and continue with the assigned bug reproducer. If the assigned reproducer already passes on the current branch before any edits, do not manufacture a failure; instead collect proof-of-already-fixed-on-branch, run an adjacent confidence check, inspect the relevant code path, and close the feature with evidence.

- Milestone: `section7-deployment-vps`
- Skill: `bug-fix-worker`
- Fulfills: VAL-VPS-012
- Status: `pending`

## 90. section8-strategy-registry-backtest-runner-bug-srb-001

Fix audited bug srb-001 in Strategy Registry / Backtest Runner. Reproduce the exact failure first, add or update a failing targeted test or reproducer, implement the smallest fix for this one bug only, run focused validators, and confirm the original reproduction no longer fails. If live environment prerequisites for this bug are blocked (for example VPS or local OAuth proxy), reproduce the blocker explicitly and return with blocker evidence instead of claiming success. Do not treat unrelated mission-wide suite failures as blocking if this feature's assigned reproducer can run independently; report unrelated failures separately and continue with the assigned bug reproducer. If the assigned reproducer already passes on the current branch before any edits, do not manufacture a failure; instead collect proof-of-already-fixed-on-branch, run an adjacent confidence check, inspect the relevant code path, and close the feature with evidence.

- Milestone: `section8-strategy-registry-backtest-runner`
- Skill: `bug-fix-worker`
- Fulfills: VAL-SRB-001
- Status: `pending`

## 91. section8-strategy-registry-backtest-runner-bug-srb-002

Fix audited bug srb-002 in Strategy Registry / Backtest Runner. Reproduce the exact failure first, add or update a failing targeted test or reproducer, implement the smallest fix for this one bug only, run focused validators, and confirm the original reproduction no longer fails. If live environment prerequisites for this bug are blocked (for example VPS or local OAuth proxy), reproduce the blocker explicitly and return with blocker evidence instead of claiming success. Do not treat unrelated mission-wide suite failures as blocking if this feature's assigned reproducer can run independently; report unrelated failures separately and continue with the assigned bug reproducer. If the assigned reproducer already passes on the current branch before any edits, do not manufacture a failure; instead collect proof-of-already-fixed-on-branch, run an adjacent confidence check, inspect the relevant code path, and close the feature with evidence.

- Milestone: `section8-strategy-registry-backtest-runner`
- Skill: `bug-fix-worker`
- Fulfills: VAL-SRB-002
- Status: `pending`

## 92. section8-strategy-registry-backtest-runner-bug-srb-003

Fix audited bug srb-003 in Strategy Registry / Backtest Runner. Reproduce the exact failure first, add or update a failing targeted test or reproducer, implement the smallest fix for this one bug only, run focused validators, and confirm the original reproduction no longer fails. If live environment prerequisites for this bug are blocked (for example VPS or local OAuth proxy), reproduce the blocker explicitly and return with blocker evidence instead of claiming success. Do not treat unrelated mission-wide suite failures as blocking if this feature's assigned reproducer can run independently; report unrelated failures separately and continue with the assigned bug reproducer. If the assigned reproducer already passes on the current branch before any edits, do not manufacture a failure; instead collect proof-of-already-fixed-on-branch, run an adjacent confidence check, inspect the relevant code path, and close the feature with evidence.

- Milestone: `section8-strategy-registry-backtest-runner`
- Skill: `bug-fix-worker`
- Fulfills: VAL-SRB-003
- Status: `pending`

## 93. section8-strategy-registry-backtest-runner-bug-srb-004

Fix audited bug srb-004 in Strategy Registry / Backtest Runner. Reproduce the exact failure first, add or update a failing targeted test or reproducer, implement the smallest fix for this one bug only, run focused validators, and confirm the original reproduction no longer fails. If live environment prerequisites for this bug are blocked (for example VPS or local OAuth proxy), reproduce the blocker explicitly and return with blocker evidence instead of claiming success. Do not treat unrelated mission-wide suite failures as blocking if this feature's assigned reproducer can run independently; report unrelated failures separately and continue with the assigned bug reproducer. If the assigned reproducer already passes on the current branch before any edits, do not manufacture a failure; instead collect proof-of-already-fixed-on-branch, run an adjacent confidence check, inspect the relevant code path, and close the feature with evidence.

- Milestone: `section8-strategy-registry-backtest-runner`
- Skill: `bug-fix-worker`
- Fulfills: VAL-SRB-004
- Status: `pending`

## 94. section8-strategy-registry-backtest-runner-bug-srb-005

Fix audited bug srb-005 in Strategy Registry / Backtest Runner. Reproduce the exact failure first, add or update a failing targeted test or reproducer, implement the smallest fix for this one bug only, run focused validators, and confirm the original reproduction no longer fails. If live environment prerequisites for this bug are blocked (for example VPS or local OAuth proxy), reproduce the blocker explicitly and return with blocker evidence instead of claiming success. Do not treat unrelated mission-wide suite failures as blocking if this feature's assigned reproducer can run independently; report unrelated failures separately and continue with the assigned bug reproducer. If the assigned reproducer already passes on the current branch before any edits, do not manufacture a failure; instead collect proof-of-already-fixed-on-branch, run an adjacent confidence check, inspect the relevant code path, and close the feature with evidence.

- Milestone: `section8-strategy-registry-backtest-runner`
- Skill: `bug-fix-worker`
- Fulfills: VAL-SRB-005
- Status: `pending`

## 95. section8-strategy-registry-backtest-runner-bug-srb-006

Fix audited bug srb-006 in Strategy Registry / Backtest Runner. Reproduce the exact failure first, add or update a failing targeted test or reproducer, implement the smallest fix for this one bug only, run focused validators, and confirm the original reproduction no longer fails. If live environment prerequisites for this bug are blocked (for example VPS or local OAuth proxy), reproduce the blocker explicitly and return with blocker evidence instead of claiming success. Do not treat unrelated mission-wide suite failures as blocking if this feature's assigned reproducer can run independently; report unrelated failures separately and continue with the assigned bug reproducer. If the assigned reproducer already passes on the current branch before any edits, do not manufacture a failure; instead collect proof-of-already-fixed-on-branch, run an adjacent confidence check, inspect the relevant code path, and close the feature with evidence.

- Milestone: `section8-strategy-registry-backtest-runner`
- Skill: `bug-fix-worker`
- Fulfills: VAL-SRB-006
- Status: `pending`

## 96. section8-strategy-registry-backtest-runner-bug-srb-007

Fix audited bug srb-007 in Strategy Registry / Backtest Runner. Reproduce the exact failure first, add or update a failing targeted test or reproducer, implement the smallest fix for this one bug only, run focused validators, and confirm the original reproduction no longer fails. If live environment prerequisites for this bug are blocked (for example VPS or local OAuth proxy), reproduce the blocker explicitly and return with blocker evidence instead of claiming success. Do not treat unrelated mission-wide suite failures as blocking if this feature's assigned reproducer can run independently; report unrelated failures separately and continue with the assigned bug reproducer. If the assigned reproducer already passes on the current branch before any edits, do not manufacture a failure; instead collect proof-of-already-fixed-on-branch, run an adjacent confidence check, inspect the relevant code path, and close the feature with evidence.

- Milestone: `section8-strategy-registry-backtest-runner`
- Skill: `bug-fix-worker`
- Fulfills: VAL-SRB-007
- Status: `pending`

## 97. section8-strategy-registry-backtest-runner-bug-srb-008

Fix audited bug srb-008 in Strategy Registry / Backtest Runner. Reproduce the exact failure first, add or update a failing targeted test or reproducer, implement the smallest fix for this one bug only, run focused validators, and confirm the original reproduction no longer fails. If live environment prerequisites for this bug are blocked (for example VPS or local OAuth proxy), reproduce the blocker explicitly and return with blocker evidence instead of claiming success. Do not treat unrelated mission-wide suite failures as blocking if this feature's assigned reproducer can run independently; report unrelated failures separately and continue with the assigned bug reproducer. If the assigned reproducer already passes on the current branch before any edits, do not manufacture a failure; instead collect proof-of-already-fixed-on-branch, run an adjacent confidence check, inspect the relevant code path, and close the feature with evidence.

- Milestone: `section8-strategy-registry-backtest-runner`
- Skill: `bug-fix-worker`
- Fulfills: VAL-SRB-008
- Status: `pending`

## 98. section8-strategy-registry-backtest-runner-bug-srb-009

Fix audited bug srb-009 in Strategy Registry / Backtest Runner. Reproduce the exact failure first, add or update a failing targeted test or reproducer, implement the smallest fix for this one bug only, run focused validators, and confirm the original reproduction no longer fails. If live environment prerequisites for this bug are blocked (for example VPS or local OAuth proxy), reproduce the blocker explicitly and return with blocker evidence instead of claiming success. Do not treat unrelated mission-wide suite failures as blocking if this feature's assigned reproducer can run independently; report unrelated failures separately and continue with the assigned bug reproducer. If the assigned reproducer already passes on the current branch before any edits, do not manufacture a failure; instead collect proof-of-already-fixed-on-branch, run an adjacent confidence check, inspect the relevant code path, and close the feature with evidence.

- Milestone: `section8-strategy-registry-backtest-runner`
- Skill: `bug-fix-worker`
- Fulfills: VAL-SRB-009
- Status: `pending`

## 99. section8-strategy-registry-backtest-runner-bug-srb-010

Fix audited bug srb-010 in Strategy Registry / Backtest Runner. Reproduce the exact failure first, add or update a failing targeted test or reproducer, implement the smallest fix for this one bug only, run focused validators, and confirm the original reproduction no longer fails. If live environment prerequisites for this bug are blocked (for example VPS or local OAuth proxy), reproduce the blocker explicitly and return with blocker evidence instead of claiming success. Do not treat unrelated mission-wide suite failures as blocking if this feature's assigned reproducer can run independently; report unrelated failures separately and continue with the assigned bug reproducer. If the assigned reproducer already passes on the current branch before any edits, do not manufacture a failure; instead collect proof-of-already-fixed-on-branch, run an adjacent confidence check, inspect the relevant code path, and close the feature with evidence.

- Milestone: `section8-strategy-registry-backtest-runner`
- Skill: `bug-fix-worker`
- Fulfills: VAL-SRB-010
- Status: `pending`

## 100. section8-strategy-registry-backtest-runner-bug-srb-011

Fix audited bug srb-011 in Strategy Registry / Backtest Runner. Reproduce the exact failure first, add or update a failing targeted test or reproducer, implement the smallest fix for this one bug only, run focused validators, and confirm the original reproduction no longer fails. If live environment prerequisites for this bug are blocked (for example VPS or local OAuth proxy), reproduce the blocker explicitly and return with blocker evidence instead of claiming success. Do not treat unrelated mission-wide suite failures as blocking if this feature's assigned reproducer can run independently; report unrelated failures separately and continue with the assigned bug reproducer. If the assigned reproducer already passes on the current branch before any edits, do not manufacture a failure; instead collect proof-of-already-fixed-on-branch, run an adjacent confidence check, inspect the relevant code path, and close the feature with evidence.

- Milestone: `section8-strategy-registry-backtest-runner`
- Skill: `bug-fix-worker`
- Fulfills: VAL-SRB-011
- Status: `pending`

## 101. section8-strategy-registry-backtest-runner-bug-srb-012

Fix audited bug srb-012 in Strategy Registry / Backtest Runner. Reproduce the exact failure first, add or update a failing targeted test or reproducer, implement the smallest fix for this one bug only, run focused validators, and confirm the original reproduction no longer fails. If live environment prerequisites for this bug are blocked (for example VPS or local OAuth proxy), reproduce the blocker explicitly and return with blocker evidence instead of claiming success. Do not treat unrelated mission-wide suite failures as blocking if this feature's assigned reproducer can run independently; report unrelated failures separately and continue with the assigned bug reproducer. If the assigned reproducer already passes on the current branch before any edits, do not manufacture a failure; instead collect proof-of-already-fixed-on-branch, run an adjacent confidence check, inspect the relevant code path, and close the feature with evidence.

- Milestone: `section8-strategy-registry-backtest-runner`
- Skill: `bug-fix-worker`
- Fulfills: VAL-SRB-012
- Status: `pending`

## 102. section8-strategy-registry-backtest-runner-bug-srb-013

Fix audited bug srb-013 in Strategy Registry / Backtest Runner. Reproduce the exact failure first, add or update a failing targeted test or reproducer, implement the smallest fix for this one bug only, run focused validators, and confirm the original reproduction no longer fails. If live environment prerequisites for this bug are blocked (for example VPS or local OAuth proxy), reproduce the blocker explicitly and return with blocker evidence instead of claiming success. Do not treat unrelated mission-wide suite failures as blocking if this feature's assigned reproducer can run independently; report unrelated failures separately and continue with the assigned bug reproducer. If the assigned reproducer already passes on the current branch before any edits, do not manufacture a failure; instead collect proof-of-already-fixed-on-branch, run an adjacent confidence check, inspect the relevant code path, and close the feature with evidence.

- Milestone: `section8-strategy-registry-backtest-runner`
- Skill: `bug-fix-worker`
- Fulfills: VAL-SRB-013
- Status: `pending`

## 103. section9-ema-strategy-bug-ema-001

Fix audited bug ema-001 in EMA Strategy. Reproduce the exact failure first, add or update a failing targeted test or reproducer, implement the smallest fix for this one bug only, run focused validators, and confirm the original reproduction no longer fails. If live environment prerequisites for this bug are blocked (for example VPS or local OAuth proxy), reproduce the blocker explicitly and return with blocker evidence instead of claiming success. Do not treat unrelated mission-wide suite failures as blocking if this feature's assigned reproducer can run independently; report unrelated failures separately and continue with the assigned bug reproducer. If the assigned reproducer already passes on the current branch before any edits, do not manufacture a failure; instead collect proof-of-already-fixed-on-branch, run an adjacent confidence check, inspect the relevant code path, and close the feature with evidence.

- Milestone: `section9-ema-strategy`
- Skill: `bug-fix-worker`
- Fulfills: VAL-EMA-001
- Status: `pending`

## 104. section9-ema-strategy-bug-ema-002

Fix audited bug ema-002 in EMA Strategy. Reproduce the exact failure first, add or update a failing targeted test or reproducer, implement the smallest fix for this one bug only, run focused validators, and confirm the original reproduction no longer fails. If live environment prerequisites for this bug are blocked (for example VPS or local OAuth proxy), reproduce the blocker explicitly and return with blocker evidence instead of claiming success. Do not treat unrelated mission-wide suite failures as blocking if this feature's assigned reproducer can run independently; report unrelated failures separately and continue with the assigned bug reproducer. If the assigned reproducer already passes on the current branch before any edits, do not manufacture a failure; instead collect proof-of-already-fixed-on-branch, run an adjacent confidence check, inspect the relevant code path, and close the feature with evidence.

- Milestone: `section9-ema-strategy`
- Skill: `bug-fix-worker`
- Fulfills: VAL-EMA-002
- Status: `pending`

## 105. section9-ema-strategy-bug-ema-003

Fix audited bug ema-003 in EMA Strategy. Reproduce the exact failure first, add or update a failing targeted test or reproducer, implement the smallest fix for this one bug only, run focused validators, and confirm the original reproduction no longer fails. If live environment prerequisites for this bug are blocked (for example VPS or local OAuth proxy), reproduce the blocker explicitly and return with blocker evidence instead of claiming success. Do not treat unrelated mission-wide suite failures as blocking if this feature's assigned reproducer can run independently; report unrelated failures separately and continue with the assigned bug reproducer. If the assigned reproducer already passes on the current branch before any edits, do not manufacture a failure; instead collect proof-of-already-fixed-on-branch, run an adjacent confidence check, inspect the relevant code path, and close the feature with evidence.

- Milestone: `section9-ema-strategy`
- Skill: `bug-fix-worker`
- Fulfills: VAL-EMA-003
- Status: `pending`

## 106. section9-ema-strategy-bug-ema-004

Fix audited bug ema-004 in EMA Strategy. Reproduce the exact failure first, add or update a failing targeted test or reproducer, implement the smallest fix for this one bug only, run focused validators, and confirm the original reproduction no longer fails. If live environment prerequisites for this bug are blocked (for example VPS or local OAuth proxy), reproduce the blocker explicitly and return with blocker evidence instead of claiming success. Do not treat unrelated mission-wide suite failures as blocking if this feature's assigned reproducer can run independently; report unrelated failures separately and continue with the assigned bug reproducer. If the assigned reproducer already passes on the current branch before any edits, do not manufacture a failure; instead collect proof-of-already-fixed-on-branch, run an adjacent confidence check, inspect the relevant code path, and close the feature with evidence.

- Milestone: `section9-ema-strategy`
- Skill: `bug-fix-worker`
- Fulfills: VAL-EMA-004
- Status: `pending`

## 107. section9-ema-strategy-bug-ema-005

Fix audited bug ema-005 in EMA Strategy. Reproduce the exact failure first, add or update a failing targeted test or reproducer, implement the smallest fix for this one bug only, run focused validators, and confirm the original reproduction no longer fails. If live environment prerequisites for this bug are blocked (for example VPS or local OAuth proxy), reproduce the blocker explicitly and return with blocker evidence instead of claiming success. Do not treat unrelated mission-wide suite failures as blocking if this feature's assigned reproducer can run independently; report unrelated failures separately and continue with the assigned bug reproducer. If the assigned reproducer already passes on the current branch before any edits, do not manufacture a failure; instead collect proof-of-already-fixed-on-branch, run an adjacent confidence check, inspect the relevant code path, and close the feature with evidence.

- Milestone: `section9-ema-strategy`
- Skill: `bug-fix-worker`
- Fulfills: VAL-EMA-005
- Status: `pending`

## 108. section9-ema-strategy-bug-ema-006

Fix audited bug ema-006 in EMA Strategy. Reproduce the exact failure first, add or update a failing targeted test or reproducer, implement the smallest fix for this one bug only, run focused validators, and confirm the original reproduction no longer fails. If live environment prerequisites for this bug are blocked (for example VPS or local OAuth proxy), reproduce the blocker explicitly and return with blocker evidence instead of claiming success. Do not treat unrelated mission-wide suite failures as blocking if this feature's assigned reproducer can run independently; report unrelated failures separately and continue with the assigned bug reproducer. If the assigned reproducer already passes on the current branch before any edits, do not manufacture a failure; instead collect proof-of-already-fixed-on-branch, run an adjacent confidence check, inspect the relevant code path, and close the feature with evidence.

- Milestone: `section9-ema-strategy`
- Skill: `bug-fix-worker`
- Fulfills: VAL-EMA-006
- Status: `pending`

## 109. section9-ema-strategy-bug-ema-007

Fix audited bug ema-007 in EMA Strategy. Reproduce the exact failure first, add or update a failing targeted test or reproducer, implement the smallest fix for this one bug only, run focused validators, and confirm the original reproduction no longer fails. If live environment prerequisites for this bug are blocked (for example VPS or local OAuth proxy), reproduce the blocker explicitly and return with blocker evidence instead of claiming success. Do not treat unrelated mission-wide suite failures as blocking if this feature's assigned reproducer can run independently; report unrelated failures separately and continue with the assigned bug reproducer. If the assigned reproducer already passes on the current branch before any edits, do not manufacture a failure; instead collect proof-of-already-fixed-on-branch, run an adjacent confidence check, inspect the relevant code path, and close the feature with evidence.

- Milestone: `section9-ema-strategy`
- Skill: `bug-fix-worker`
- Fulfills: VAL-EMA-007
- Status: `pending`

## 110. section9-ema-strategy-bug-ema-008

Fix audited bug ema-008 in EMA Strategy. Reproduce the exact failure first, add or update a failing targeted test or reproducer, implement the smallest fix for this one bug only, run focused validators, and confirm the original reproduction no longer fails. If live environment prerequisites for this bug are blocked (for example VPS or local OAuth proxy), reproduce the blocker explicitly and return with blocker evidence instead of claiming success. Do not treat unrelated mission-wide suite failures as blocking if this feature's assigned reproducer can run independently; report unrelated failures separately and continue with the assigned bug reproducer. If the assigned reproducer already passes on the current branch before any edits, do not manufacture a failure; instead collect proof-of-already-fixed-on-branch, run an adjacent confidence check, inspect the relevant code path, and close the feature with evidence.

- Milestone: `section9-ema-strategy`
- Skill: `bug-fix-worker`
- Fulfills: VAL-EMA-008
- Status: `pending`

## 111. section9-ema-strategy-bug-ema-009

Fix audited bug ema-009 in EMA Strategy. Reproduce the exact failure first, add or update a failing targeted test or reproducer, implement the smallest fix for this one bug only, run focused validators, and confirm the original reproduction no longer fails. If live environment prerequisites for this bug are blocked (for example VPS or local OAuth proxy), reproduce the blocker explicitly and return with blocker evidence instead of claiming success. Do not treat unrelated mission-wide suite failures as blocking if this feature's assigned reproducer can run independently; report unrelated failures separately and continue with the assigned bug reproducer. If the assigned reproducer already passes on the current branch before any edits, do not manufacture a failure; instead collect proof-of-already-fixed-on-branch, run an adjacent confidence check, inspect the relevant code path, and close the feature with evidence.

- Milestone: `section9-ema-strategy`
- Skill: `bug-fix-worker`
- Fulfills: VAL-EMA-009
- Status: `pending`

## 112. section9-ema-strategy-bug-ema-010

Fix audited bug ema-010 in EMA Strategy. Reproduce the exact failure first, add or update a failing targeted test or reproducer, implement the smallest fix for this one bug only, run focused validators, and confirm the original reproduction no longer fails. If live environment prerequisites for this bug are blocked (for example VPS or local OAuth proxy), reproduce the blocker explicitly and return with blocker evidence instead of claiming success. Do not treat unrelated mission-wide suite failures as blocking if this feature's assigned reproducer can run independently; report unrelated failures separately and continue with the assigned bug reproducer. If the assigned reproducer already passes on the current branch before any edits, do not manufacture a failure; instead collect proof-of-already-fixed-on-branch, run an adjacent confidence check, inspect the relevant code path, and close the feature with evidence.

- Milestone: `section9-ema-strategy`
- Skill: `bug-fix-worker`
- Fulfills: VAL-EMA-010
- Status: `pending`

## 113. section9-ema-strategy-bug-ema-011

Fix audited bug ema-011 in EMA Strategy. Reproduce the exact failure first, add or update a failing targeted test or reproducer, implement the smallest fix for this one bug only, run focused validators, and confirm the original reproduction no longer fails. If live environment prerequisites for this bug are blocked (for example VPS or local OAuth proxy), reproduce the blocker explicitly and return with blocker evidence instead of claiming success. Do not treat unrelated mission-wide suite failures as blocking if this feature's assigned reproducer can run independently; report unrelated failures separately and continue with the assigned bug reproducer. If the assigned reproducer already passes on the current branch before any edits, do not manufacture a failure; instead collect proof-of-already-fixed-on-branch, run an adjacent confidence check, inspect the relevant code path, and close the feature with evidence.

- Milestone: `section9-ema-strategy`
- Skill: `bug-fix-worker`
- Fulfills: VAL-EMA-011
- Status: `pending`

## 114. section9-ema-strategy-bug-ema-012

Fix audited bug ema-012 in EMA Strategy. Reproduce the exact failure first, add or update a failing targeted test or reproducer, implement the smallest fix for this one bug only, run focused validators, and confirm the original reproduction no longer fails. If live environment prerequisites for this bug are blocked (for example VPS or local OAuth proxy), reproduce the blocker explicitly and return with blocker evidence instead of claiming success. Do not treat unrelated mission-wide suite failures as blocking if this feature's assigned reproducer can run independently; report unrelated failures separately and continue with the assigned bug reproducer. If the assigned reproducer already passes on the current branch before any edits, do not manufacture a failure; instead collect proof-of-already-fixed-on-branch, run an adjacent confidence check, inspect the relevant code path, and close the feature with evidence.

- Milestone: `section9-ema-strategy`
- Skill: `bug-fix-worker`
- Fulfills: VAL-EMA-012
- Status: `pending`

## 115. section10-orb-strategy-bug-orb-001

Fix audited bug orb-001 in ORB Strategy. Reproduce the exact failure first, add or update a failing targeted test or reproducer, implement the smallest fix for this one bug only, run focused validators, and confirm the original reproduction no longer fails. If live environment prerequisites for this bug are blocked (for example VPS or local OAuth proxy), reproduce the blocker explicitly and return with blocker evidence instead of claiming success. Do not treat unrelated mission-wide suite failures as blocking if this feature's assigned reproducer can run independently; report unrelated failures separately and continue with the assigned bug reproducer. If the assigned reproducer already passes on the current branch before any edits, do not manufacture a failure; instead collect proof-of-already-fixed-on-branch, run an adjacent confidence check, inspect the relevant code path, and close the feature with evidence.

- Milestone: `section10-orb-strategy`
- Skill: `bug-fix-worker`
- Fulfills: VAL-ORB-001
- Status: `pending`

## 116. section10-orb-strategy-bug-orb-002

Fix audited bug orb-002 in ORB Strategy. Reproduce the exact failure first, add or update a failing targeted test or reproducer, implement the smallest fix for this one bug only, run focused validators, and confirm the original reproduction no longer fails. If live environment prerequisites for this bug are blocked (for example VPS or local OAuth proxy), reproduce the blocker explicitly and return with blocker evidence instead of claiming success. Do not treat unrelated mission-wide suite failures as blocking if this feature's assigned reproducer can run independently; report unrelated failures separately and continue with the assigned bug reproducer. If the assigned reproducer already passes on the current branch before any edits, do not manufacture a failure; instead collect proof-of-already-fixed-on-branch, run an adjacent confidence check, inspect the relevant code path, and close the feature with evidence.

- Milestone: `section10-orb-strategy`
- Skill: `bug-fix-worker`
- Fulfills: VAL-ORB-002
- Status: `pending`

## 117. section10-orb-strategy-bug-orb-003

Fix audited bug orb-003 in ORB Strategy. Reproduce the exact failure first, add or update a failing targeted test or reproducer, implement the smallest fix for this one bug only, run focused validators, and confirm the original reproduction no longer fails. If live environment prerequisites for this bug are blocked (for example VPS or local OAuth proxy), reproduce the blocker explicitly and return with blocker evidence instead of claiming success. Do not treat unrelated mission-wide suite failures as blocking if this feature's assigned reproducer can run independently; report unrelated failures separately and continue with the assigned bug reproducer. If the assigned reproducer already passes on the current branch before any edits, do not manufacture a failure; instead collect proof-of-already-fixed-on-branch, run an adjacent confidence check, inspect the relevant code path, and close the feature with evidence.

- Milestone: `section10-orb-strategy`
- Skill: `bug-fix-worker`
- Fulfills: VAL-ORB-003
- Status: `pending`

## 118. section10-orb-strategy-bug-orb-004

Fix audited bug orb-004 in ORB Strategy. Reproduce the exact failure first, add or update a failing targeted test or reproducer, implement the smallest fix for this one bug only, run focused validators, and confirm the original reproduction no longer fails. If live environment prerequisites for this bug are blocked (for example VPS or local OAuth proxy), reproduce the blocker explicitly and return with blocker evidence instead of claiming success. Do not treat unrelated mission-wide suite failures as blocking if this feature's assigned reproducer can run independently; report unrelated failures separately and continue with the assigned bug reproducer. If the assigned reproducer already passes on the current branch before any edits, do not manufacture a failure; instead collect proof-of-already-fixed-on-branch, run an adjacent confidence check, inspect the relevant code path, and close the feature with evidence.

- Milestone: `section10-orb-strategy`
- Skill: `bug-fix-worker`
- Fulfills: VAL-ORB-004
- Status: `pending`

## 119. section10-orb-strategy-bug-orb-005

Fix audited bug orb-005 in ORB Strategy. Reproduce the exact failure first, add or update a failing targeted test or reproducer, implement the smallest fix for this one bug only, run focused validators, and confirm the original reproduction no longer fails. If live environment prerequisites for this bug are blocked (for example VPS or local OAuth proxy), reproduce the blocker explicitly and return with blocker evidence instead of claiming success. Do not treat unrelated mission-wide suite failures as blocking if this feature's assigned reproducer can run independently; report unrelated failures separately and continue with the assigned bug reproducer. If the assigned reproducer already passes on the current branch before any edits, do not manufacture a failure; instead collect proof-of-already-fixed-on-branch, run an adjacent confidence check, inspect the relevant code path, and close the feature with evidence.

- Milestone: `section10-orb-strategy`
- Skill: `bug-fix-worker`
- Fulfills: VAL-ORB-005
- Status: `pending`

## 120. section10-orb-strategy-bug-orb-006

Fix audited bug orb-006 in ORB Strategy. Reproduce the exact failure first, add or update a failing targeted test or reproducer, implement the smallest fix for this one bug only, run focused validators, and confirm the original reproduction no longer fails. If live environment prerequisites for this bug are blocked (for example VPS or local OAuth proxy), reproduce the blocker explicitly and return with blocker evidence instead of claiming success. Do not treat unrelated mission-wide suite failures as blocking if this feature's assigned reproducer can run independently; report unrelated failures separately and continue with the assigned bug reproducer. If the assigned reproducer already passes on the current branch before any edits, do not manufacture a failure; instead collect proof-of-already-fixed-on-branch, run an adjacent confidence check, inspect the relevant code path, and close the feature with evidence.

- Milestone: `section10-orb-strategy`
- Skill: `bug-fix-worker`
- Fulfills: VAL-ORB-006
- Status: `pending`

## 121. section10-orb-strategy-bug-orb-007

Fix audited bug orb-007 in ORB Strategy. Reproduce the exact failure first, add or update a failing targeted test or reproducer, implement the smallest fix for this one bug only, run focused validators, and confirm the original reproduction no longer fails. If live environment prerequisites for this bug are blocked (for example VPS or local OAuth proxy), reproduce the blocker explicitly and return with blocker evidence instead of claiming success. Do not treat unrelated mission-wide suite failures as blocking if this feature's assigned reproducer can run independently; report unrelated failures separately and continue with the assigned bug reproducer. If the assigned reproducer already passes on the current branch before any edits, do not manufacture a failure; instead collect proof-of-already-fixed-on-branch, run an adjacent confidence check, inspect the relevant code path, and close the feature with evidence.

- Milestone: `section10-orb-strategy`
- Skill: `bug-fix-worker`
- Fulfills: VAL-ORB-007
- Status: `pending`

## 122. section10-orb-strategy-bug-orb-008

Fix audited bug orb-008 in ORB Strategy. Reproduce the exact failure first, add or update a failing targeted test or reproducer, implement the smallest fix for this one bug only, run focused validators, and confirm the original reproduction no longer fails. If live environment prerequisites for this bug are blocked (for example VPS or local OAuth proxy), reproduce the blocker explicitly and return with blocker evidence instead of claiming success. Do not treat unrelated mission-wide suite failures as blocking if this feature's assigned reproducer can run independently; report unrelated failures separately and continue with the assigned bug reproducer. If the assigned reproducer already passes on the current branch before any edits, do not manufacture a failure; instead collect proof-of-already-fixed-on-branch, run an adjacent confidence check, inspect the relevant code path, and close the feature with evidence.

- Milestone: `section10-orb-strategy`
- Skill: `bug-fix-worker`
- Fulfills: VAL-ORB-008
- Status: `pending`

## 123. section10-orb-strategy-bug-orb-009

Fix audited bug orb-009 in ORB Strategy. Reproduce the exact failure first, add or update a failing targeted test or reproducer, implement the smallest fix for this one bug only, run focused validators, and confirm the original reproduction no longer fails. If live environment prerequisites for this bug are blocked (for example VPS or local OAuth proxy), reproduce the blocker explicitly and return with blocker evidence instead of claiming success. Do not treat unrelated mission-wide suite failures as blocking if this feature's assigned reproducer can run independently; report unrelated failures separately and continue with the assigned bug reproducer. If the assigned reproducer already passes on the current branch before any edits, do not manufacture a failure; instead collect proof-of-already-fixed-on-branch, run an adjacent confidence check, inspect the relevant code path, and close the feature with evidence.

- Milestone: `section10-orb-strategy`
- Skill: `bug-fix-worker`
- Fulfills: VAL-ORB-009
- Status: `pending`

## 124. section10-orb-strategy-bug-orb-010

Fix audited bug orb-010 in ORB Strategy. Reproduce the exact failure first, add or update a failing targeted test or reproducer, implement the smallest fix for this one bug only, run focused validators, and confirm the original reproduction no longer fails. If live environment prerequisites for this bug are blocked (for example VPS or local OAuth proxy), reproduce the blocker explicitly and return with blocker evidence instead of claiming success. Do not treat unrelated mission-wide suite failures as blocking if this feature's assigned reproducer can run independently; report unrelated failures separately and continue with the assigned bug reproducer. If the assigned reproducer already passes on the current branch before any edits, do not manufacture a failure; instead collect proof-of-already-fixed-on-branch, run an adjacent confidence check, inspect the relevant code path, and close the feature with evidence.

- Milestone: `section10-orb-strategy`
- Skill: `bug-fix-worker`
- Fulfills: VAL-ORB-010
- Status: `pending`

## 125. section10-orb-strategy-bug-orb-011

Fix audited bug orb-011 in ORB Strategy. Reproduce the exact failure first, add or update a failing targeted test or reproducer, implement the smallest fix for this one bug only, run focused validators, and confirm the original reproduction no longer fails. If live environment prerequisites for this bug are blocked (for example VPS or local OAuth proxy), reproduce the blocker explicitly and return with blocker evidence instead of claiming success. Do not treat unrelated mission-wide suite failures as blocking if this feature's assigned reproducer can run independently; report unrelated failures separately and continue with the assigned bug reproducer. If the assigned reproducer already passes on the current branch before any edits, do not manufacture a failure; instead collect proof-of-already-fixed-on-branch, run an adjacent confidence check, inspect the relevant code path, and close the feature with evidence.

- Milestone: `section10-orb-strategy`
- Skill: `bug-fix-worker`
- Fulfills: VAL-ORB-011
- Status: `pending`

## 126. section10-orb-strategy-bug-orb-012

Fix audited bug orb-012 in ORB Strategy. Reproduce the exact failure first, add or update a failing targeted test or reproducer, implement the smallest fix for this one bug only, run focused validators, and confirm the original reproduction no longer fails. If live environment prerequisites for this bug are blocked (for example VPS or local OAuth proxy), reproduce the blocker explicitly and return with blocker evidence instead of claiming success. Do not treat unrelated mission-wide suite failures as blocking if this feature's assigned reproducer can run independently; report unrelated failures separately and continue with the assigned bug reproducer. If the assigned reproducer already passes on the current branch before any edits, do not manufacture a failure; instead collect proof-of-already-fixed-on-branch, run an adjacent confidence check, inspect the relevant code path, and close the feature with evidence.

- Milestone: `section10-orb-strategy`
- Skill: `bug-fix-worker`
- Fulfills: VAL-ORB-012
- Status: `pending`

## 127. section10-orb-strategy-bug-orb-013

Fix audited bug orb-013 in ORB Strategy. Reproduce the exact failure first, add or update a failing targeted test or reproducer, implement the smallest fix for this one bug only, run focused validators, and confirm the original reproduction no longer fails. If live environment prerequisites for this bug are blocked (for example VPS or local OAuth proxy), reproduce the blocker explicitly and return with blocker evidence instead of claiming success. Do not treat unrelated mission-wide suite failures as blocking if this feature's assigned reproducer can run independently; report unrelated failures separately and continue with the assigned bug reproducer. If the assigned reproducer already passes on the current branch before any edits, do not manufacture a failure; instead collect proof-of-already-fixed-on-branch, run an adjacent confidence check, inspect the relevant code path, and close the feature with evidence.

- Milestone: `section10-orb-strategy`
- Skill: `bug-fix-worker`
- Fulfills: VAL-ORB-013
- Status: `pending`

## 128. section10-orb-strategy-bug-orb-014

Fix audited bug orb-014 in ORB Strategy. Reproduce the exact failure first, add or update a failing targeted test or reproducer, implement the smallest fix for this one bug only, run focused validators, and confirm the original reproduction no longer fails. If live environment prerequisites for this bug are blocked (for example VPS or local OAuth proxy), reproduce the blocker explicitly and return with blocker evidence instead of claiming success. Do not treat unrelated mission-wide suite failures as blocking if this feature's assigned reproducer can run independently; report unrelated failures separately and continue with the assigned bug reproducer. If the assigned reproducer already passes on the current branch before any edits, do not manufacture a failure; instead collect proof-of-already-fixed-on-branch, run an adjacent confidence check, inspect the relevant code path, and close the feature with evidence.

- Milestone: `section10-orb-strategy`
- Skill: `bug-fix-worker`
- Fulfills: VAL-ORB-014
- Status: `pending`

## 129. section10-orb-strategy-bug-orb-015

Fix audited bug orb-015 in ORB Strategy. Reproduce the exact failure first, add or update a failing targeted test or reproducer, implement the smallest fix for this one bug only, run focused validators, and confirm the original reproduction no longer fails. If live environment prerequisites for this bug are blocked (for example VPS or local OAuth proxy), reproduce the blocker explicitly and return with blocker evidence instead of claiming success. Do not treat unrelated mission-wide suite failures as blocking if this feature's assigned reproducer can run independently; report unrelated failures separately and continue with the assigned bug reproducer. If the assigned reproducer already passes on the current branch before any edits, do not manufacture a failure; instead collect proof-of-already-fixed-on-branch, run an adjacent confidence check, inspect the relevant code path, and close the feature with evidence.

- Milestone: `section10-orb-strategy`
- Skill: `bug-fix-worker`
- Fulfills: VAL-ORB-015
- Status: `pending`

## 130. section10-orb-strategy-cross-area-001

Verify and fix cross-area integration assertion VAL-XA-001. Reproduce the cross-boundary failure first, implement only the integration fix required for this assertion, and confirm the full cross-area flow passes end-to-end. Do not treat unrelated mission-wide suite failures as blocking if this feature's assigned reproducer can run independently; report unrelated failures separately and continue with the assigned bug reproducer. If the assigned reproducer already passes on the current branch before any edits, do not manufacture a failure; instead collect proof-of-already-fixed-on-branch, run an adjacent confidence check, inspect the relevant code path, and close the feature with evidence.

- Milestone: `section10-orb-strategy`
- Skill: `bug-fix-worker`
- Fulfills: VAL-XA-001
- Status: `pending`

## 131. section10-orb-strategy-cross-area-002

Verify and fix cross-area integration assertion VAL-XA-002. Reproduce the cross-boundary failure first, implement only the integration fix required for this assertion, and confirm the full cross-area flow passes end-to-end. Do not treat unrelated mission-wide suite failures as blocking if this feature's assigned reproducer can run independently; report unrelated failures separately and continue with the assigned bug reproducer. If the assigned reproducer already passes on the current branch before any edits, do not manufacture a failure; instead collect proof-of-already-fixed-on-branch, run an adjacent confidence check, inspect the relevant code path, and close the feature with evidence.

- Milestone: `section10-orb-strategy`
- Skill: `bug-fix-worker`
- Fulfills: VAL-XA-002
- Status: `pending`

## 132. section10-orb-strategy-cross-area-003

Verify and fix cross-area integration assertion VAL-XA-003. Reproduce the cross-boundary failure first, implement only the integration fix required for this assertion, and confirm the full cross-area flow passes end-to-end. Do not treat unrelated mission-wide suite failures as blocking if this feature's assigned reproducer can run independently; report unrelated failures separately and continue with the assigned bug reproducer. If the assigned reproducer already passes on the current branch before any edits, do not manufacture a failure; instead collect proof-of-already-fixed-on-branch, run an adjacent confidence check, inspect the relevant code path, and close the feature with evidence.

- Milestone: `section10-orb-strategy`
- Skill: `bug-fix-worker`
- Fulfills: VAL-XA-003
- Status: `pending`

## 133. section10-orb-strategy-cross-area-004

Verify and fix cross-area integration assertion VAL-XA-004. Reproduce the cross-boundary failure first, implement only the integration fix required for this assertion, and confirm the full cross-area flow passes end-to-end. Do not treat unrelated mission-wide suite failures as blocking if this feature's assigned reproducer can run independently; report unrelated failures separately and continue with the assigned bug reproducer. If the assigned reproducer already passes on the current branch before any edits, do not manufacture a failure; instead collect proof-of-already-fixed-on-branch, run an adjacent confidence check, inspect the relevant code path, and close the feature with evidence.

- Milestone: `section10-orb-strategy`
- Skill: `bug-fix-worker`
- Fulfills: VAL-XA-004
- Status: `pending`

## 134. section10-orb-strategy-cross-area-005

Verify and fix cross-area integration assertion VAL-XA-005. Reproduce the cross-boundary failure first, implement only the integration fix required for this assertion, and confirm the full cross-area flow passes end-to-end. Do not treat unrelated mission-wide suite failures as blocking if this feature's assigned reproducer can run independently; report unrelated failures separately and continue with the assigned bug reproducer. If the assigned reproducer already passes on the current branch before any edits, do not manufacture a failure; instead collect proof-of-already-fixed-on-branch, run an adjacent confidence check, inspect the relevant code path, and close the feature with evidence.

- Milestone: `section10-orb-strategy`
- Skill: `bug-fix-worker`
- Fulfills: VAL-XA-005
- Status: `pending`

## 135. section10-orb-strategy-cross-area-006

Verify and fix cross-area integration assertion VAL-XA-006. Reproduce the cross-boundary failure first, implement only the integration fix required for this assertion, and confirm the full cross-area flow passes end-to-end. Do not treat unrelated mission-wide suite failures as blocking if this feature's assigned reproducer can run independently; report unrelated failures separately and continue with the assigned bug reproducer. If the assigned reproducer already passes on the current branch before any edits, do not manufacture a failure; instead collect proof-of-already-fixed-on-branch, run an adjacent confidence check, inspect the relevant code path, and close the feature with evidence.

- Milestone: `section10-orb-strategy`
- Skill: `bug-fix-worker`
- Fulfills: VAL-XA-006
- Status: `pending`

## 136. section10-orb-strategy-cross-area-007

Verify and fix cross-area integration assertion VAL-XA-007. Reproduce the cross-boundary failure first, implement only the integration fix required for this assertion, and confirm the full cross-area flow passes end-to-end. Do not treat unrelated mission-wide suite failures as blocking if this feature's assigned reproducer can run independently; report unrelated failures separately and continue with the assigned bug reproducer. If the assigned reproducer already passes on the current branch before any edits, do not manufacture a failure; instead collect proof-of-already-fixed-on-branch, run an adjacent confidence check, inspect the relevant code path, and close the feature with evidence.

- Milestone: `section10-orb-strategy`
- Skill: `bug-fix-worker`
- Fulfills: VAL-XA-007
- Status: `pending`

## 137. section10-orb-strategy-cross-area-008

Verify and fix cross-area integration assertion VAL-XA-008. Reproduce the cross-boundary failure first, implement only the integration fix required for this assertion, and confirm the full cross-area flow passes end-to-end. Do not treat unrelated mission-wide suite failures as blocking if this feature's assigned reproducer can run independently; report unrelated failures separately and continue with the assigned bug reproducer. If the assigned reproducer already passes on the current branch before any edits, do not manufacture a failure; instead collect proof-of-already-fixed-on-branch, run an adjacent confidence check, inspect the relevant code path, and close the feature with evidence.

- Milestone: `section10-orb-strategy`
- Skill: `bug-fix-worker`
- Fulfills: VAL-XA-008
- Status: `pending`

## 138. section10-orb-strategy-cross-area-601

Verify and fix cross-area integration assertion VAL-XA-601. Reproduce the cross-boundary failure first, implement only the integration fix required for this assertion, and confirm the full cross-area flow passes end-to-end. Do not treat unrelated mission-wide suite failures as blocking if this feature's assigned reproducer can run independently; report unrelated failures separately and continue with the assigned bug reproducer. If the assigned reproducer already passes on the current branch before any edits, do not manufacture a failure; instead collect proof-of-already-fixed-on-branch, run an adjacent confidence check, inspect the relevant code path, and close the feature with evidence.

- Milestone: `section10-orb-strategy`
- Skill: `bug-fix-worker`
- Fulfills: VAL-XA-601
- Status: `pending`

## 139. section10-orb-strategy-cross-area-602

Verify and fix cross-area integration assertion VAL-XA-602. Reproduce the cross-boundary failure first, implement only the integration fix required for this assertion, and confirm the full cross-area flow passes end-to-end. Do not treat unrelated mission-wide suite failures as blocking if this feature's assigned reproducer can run independently; report unrelated failures separately and continue with the assigned bug reproducer. If the assigned reproducer already passes on the current branch before any edits, do not manufacture a failure; instead collect proof-of-already-fixed-on-branch, run an adjacent confidence check, inspect the relevant code path, and close the feature with evidence.

- Milestone: `section10-orb-strategy`
- Skill: `bug-fix-worker`
- Fulfills: VAL-XA-602
- Status: `pending`

## 140. section10-orb-strategy-cross-area-603

Verify and fix cross-area integration assertion VAL-XA-603. Reproduce the cross-boundary failure first, implement only the integration fix required for this assertion, and confirm the full cross-area flow passes end-to-end. Do not treat unrelated mission-wide suite failures as blocking if this feature's assigned reproducer can run independently; report unrelated failures separately and continue with the assigned bug reproducer. If the assigned reproducer already passes on the current branch before any edits, do not manufacture a failure; instead collect proof-of-already-fixed-on-branch, run an adjacent confidence check, inspect the relevant code path, and close the feature with evidence.

- Milestone: `section10-orb-strategy`
- Skill: `bug-fix-worker`
- Fulfills: VAL-XA-603
- Status: `pending`

## 141. section10-orb-strategy-cross-area-604

Verify and fix cross-area integration assertion VAL-XA-604. Reproduce the cross-boundary failure first, implement only the integration fix required for this assertion, and confirm the full cross-area flow passes end-to-end. Do not treat unrelated mission-wide suite failures as blocking if this feature's assigned reproducer can run independently; report unrelated failures separately and continue with the assigned bug reproducer. If the assigned reproducer already passes on the current branch before any edits, do not manufacture a failure; instead collect proof-of-already-fixed-on-branch, run an adjacent confidence check, inspect the relevant code path, and close the feature with evidence.

- Milestone: `section10-orb-strategy`
- Skill: `bug-fix-worker`
- Fulfills: VAL-XA-604
- Status: `pending`

## 142. section1-research-loop-orchestration-bug-rlo-003

Fix audited bug rlo-003 in Research Loop Orchestration. Reproduce the exact failure first, add or update a failing targeted test or reproducer, implement the smallest fix for this one bug only, run focused validators, and confirm the original reproduction no longer fails. If live environment prerequisites for this bug are blocked (for example VPS or local OAuth proxy), reproduce the blocker explicitly and return with blocker evidence instead of claiming success. Do not treat unrelated mission-wide suite failures as blocking if this feature's assigned reproducer can run independently; report unrelated failures separately and continue with the assigned bug reproducer. If the assigned reproducer already passes on the current branch before any edits, do not manufacture a failure; instead collect proof-of-already-fixed-on-branch, run an adjacent confidence check, inspect the relevant code path, and close the feature with evidence.

- Milestone: `section1-research-loop-orchestration`
- Skill: `bug-fix-worker`
- Fulfills: VAL-RLO-003
- Status: `completed`

## 143. section1-research-loop-orchestration-bug-rlo-001

Fix audited bug rlo-001 in Research Loop Orchestration. Reproduce the exact failure first, add or update a failing targeted test or reproducer, implement the smallest fix for this one bug only, run focused validators, and confirm the original reproduction no longer fails. If live environment prerequisites for this bug are blocked (for example VPS or local OAuth proxy), reproduce the blocker explicitly and return with blocker evidence instead of claiming success. Do not treat unrelated mission-wide suite failures as blocking if this feature's assigned reproducer can run independently; report unrelated failures separately and continue with the assigned bug reproducer. If the assigned reproducer already passes on the current branch before any edits, do not manufacture a failure; instead collect proof-of-already-fixed-on-branch, run an adjacent confidence check, inspect the relevant code path, and close the feature with evidence.

- Milestone: `section1-research-loop-orchestration`
- Skill: `bug-fix-worker`
- Fulfills: VAL-RLO-001
- Status: `completed`

## 144. section1-research-loop-orchestration-bug-rlo-002

Fix audited bug rlo-002 in Research Loop Orchestration. Reproduce the exact failure first, add or update a failing targeted test or reproducer, implement the smallest fix for this one bug only, run focused validators, and confirm the original reproduction no longer fails. If live environment prerequisites for this bug are blocked (for example VPS or local OAuth proxy), reproduce the blocker explicitly and return with blocker evidence instead of claiming success. Do not treat unrelated mission-wide suite failures as blocking if this feature's assigned reproducer can run independently; report unrelated failures separately and continue with the assigned bug reproducer. If the assigned reproducer already passes on the current branch before any edits, do not manufacture a failure; instead collect proof-of-already-fixed-on-branch, run an adjacent confidence check, inspect the relevant code path, and close the feature with evidence.

- Milestone: `section1-research-loop-orchestration`
- Skill: `bug-fix-worker`
- Fulfills: VAL-RLO-002
- Status: `completed`

## 145. section1-research-loop-orchestration-bug-rlo-004

Fix audited bug rlo-004 in Research Loop Orchestration. Reproduce the exact failure first, add or update a failing targeted test or reproducer, implement the smallest fix for this one bug only, run focused validators, and confirm the original reproduction no longer fails. If live environment prerequisites for this bug are blocked (for example VPS or local OAuth proxy), reproduce the blocker explicitly and return with blocker evidence instead of claiming success. Do not treat unrelated mission-wide suite failures as blocking if this feature's assigned reproducer can run independently; report unrelated failures separately and continue with the assigned bug reproducer. If the assigned reproducer already passes on the current branch before any edits, do not manufacture a failure; instead collect proof-of-already-fixed-on-branch, run an adjacent confidence check, inspect the relevant code path, and close the feature with evidence.

- Milestone: `section1-research-loop-orchestration`
- Skill: `bug-fix-worker`
- Fulfills: VAL-RLO-004
- Status: `completed`

## 146. section1-research-loop-orchestration-bug-rlo-005

Fix audited bug rlo-005 in Research Loop Orchestration. Reproduce the exact failure first, add or update a failing targeted test or reproducer, implement the smallest fix for this one bug only, run focused validators, and confirm the original reproduction no longer fails. If live environment prerequisites for this bug are blocked (for example VPS or local OAuth proxy), reproduce the blocker explicitly and return with blocker evidence instead of claiming success. Do not treat unrelated mission-wide suite failures as blocking if this feature's assigned reproducer can run independently; report unrelated failures separately and continue with the assigned bug reproducer. If the assigned reproducer already passes on the current branch before any edits, do not manufacture a failure; instead collect proof-of-already-fixed-on-branch, run an adjacent confidence check, inspect the relevant code path, and close the feature with evidence.

- Milestone: `section1-research-loop-orchestration`
- Skill: `bug-fix-worker`
- Fulfills: VAL-RLO-005
- Status: `completed`

## 147. section1-research-loop-orchestration-bug-rlo-006

Fix audited bug rlo-006 in Research Loop Orchestration. Reproduce the exact failure first, add or update a failing targeted test or reproducer, implement the smallest fix for this one bug only, run focused validators, and confirm the original reproduction no longer fails. If live environment prerequisites for this bug are blocked (for example VPS or local OAuth proxy), reproduce the blocker explicitly and return with blocker evidence instead of claiming success. Do not treat unrelated mission-wide suite failures as blocking if this feature's assigned reproducer can run independently; report unrelated failures separately and continue with the assigned bug reproducer. If the assigned reproducer already passes on the current branch before any edits, do not manufacture a failure; instead collect proof-of-already-fixed-on-branch, run an adjacent confidence check, inspect the relevant code path, and close the feature with evidence.

- Milestone: `section1-research-loop-orchestration`
- Skill: `bug-fix-worker`
- Fulfills: VAL-RLO-006
- Status: `completed`

