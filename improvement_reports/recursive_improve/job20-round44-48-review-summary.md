# Review Summary

Generated by: recursive-improve, report-only adaptation

## Executive Summary

- Traces reviewed: 5.
- Insights extracted: 7.
- Distinct fixes after consolidation: 6.
- Proposed fix types: 3 code_fix, 2 prompt_fix, 1 process_fix.
- Discards: 0.
- Source code modified: no.
- Branches or commits created: no.

## Top Changes

1. Deterministic analyst source paths (`code_fix`)
   - Before: Round 45 guessed `../strategy.py`, hit `Errno 2`, then timed out during recursive search.
   - After: Analyst receives exact source paths and does not need filesystem exploration.
   - Target metric delta: `duration_outlier` 20% -> 0% on similar traces; `token_usage` materially lower; `error_rate` lower.
   - Risk: low.

2. Incomplete-run finalization guard (`code_fix`)
   - Before: Round 44 exported `success=true` while ending on unresolved `run_python` tool_call.
   - After: Incomplete traces continue, retry, or export as non-success with a reason.
   - Target metric delta: `clean_success_rate` becomes truthful; future `recovery_rate` improves.
   - Risk: low.

3. Schema pre-flight plus alert-candle provenance (`prompt_fix` + `code_fix`)
   - Before: Round 47 spent several failed Python attempts before concluding alert-candle fields were absent.
   - After: Analyst stops after one validated missing-key proof; engine can later emit `alert_bar_idx` and `alert_timestamp`.
   - Target metric delta: `error_rate` lower, `token_usage` lower, `clean_success_rate` higher on signal-quality tasks.
   - Risk: prompt low; schema code medium.

## Full Prioritized Fix List

| Priority | Name | Type | Target Metrics | Risk | Effort |
|---:|---|---|---|---|---|
| 1 | Deterministic source path resolver | code_fix | `duration_outlier`, `error_rate`, `token_usage`, `clean_success_rate` | low | medium |
| 2 | Incomplete-run finalization guard | code_fix | `clean_success_rate`, `recovery_rate`, `error_rate` | low | small |
| 3 | Schema pre-flight stop rule | prompt_fix | `error_rate`, `duration_outlier`, `token_usage`, `recovery_rate` | low | small |
| 4 | Alert-candle provenance columns | code_fix | `clean_success_rate`, `error_rate`, `token_usage`, `recovery_rate` | medium | medium |
| 5 | Bounded filesystem exploration | process_fix | `duration_outlier`, `token_usage`, `error_rate` | low | small |
| 6 | Regression checks for preserved successes | prompt_fix | `clean_success_rate`, `recovery_rate` | low | small |

## Not Fixing

No proposed fix is rejected in this report. The lower-priority preservation items are deferred behind the direct failure fixes.

## Small-sample Warning

All metrics are based on five traces. Baselines and projected deltas are directional, not statistically stable.

## Traceability Chain

| Fix | Insight | Metric | Trace Evidence |
|---|---|---|---|
| Deterministic source path resolver | RI-001 | `duration_outlier`, `error_rate`, `token_usage` | Round 45 messages 38-46 and usage messages 51/56 |
| Incomplete-run finalization guard | RI-004 | `clean_success_rate`, `recovery_rate` | Round 44 final message 43 |
| Schema pre-flight stop rule | RI-002 | `error_rate`, `token_usage` | Round 47 messages 50, 56, 58, 60 |
| Alert-candle provenance | RI-003 | `clean_success_rate`, `error_rate` | Round 47 message 60 plus `signals.py`/`strategy_event_logger.py` source evidence |
| Bounded filesystem exploration | RI-005 | `duration_outlier`, `token_usage` | Round 45 message 41 timeout |
| Regression checks | RI-006, RI-007 | `clean_success_rate` | Round 46 messages 42/47; round 48 message 53 |

## Decision Status

Report-only mode stops here. No approval was requested and no implementation branch was created.
